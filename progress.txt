loopfleet — progress log

2026-07-05 — M0: Scaffold Tauri v2 app + Rust workspace
- Created Cargo workspace (resolver 2) with shared workspace.package + workspace.dependencies.
- Five library crates under crates/: loopfleet-core, loopfleet-adapters,
  loopfleet-sandbox, loopfleet-gitx, loopfleet-store (dir names match PRD: core,
  adapters, sandbox, gitx, store). Each is a minimal lib.rs with a doc comment
  stating its role and target milestone. No dependencies yet — kept intentionally
  empty to be filled in M1/M2.
- Tauri v2 app crate at src-tauri/ (package `loopfleet`): Cargo.toml (tauri 2,
  serde, serde_json + the five internal crates), build.rs (tauri_build::build),
  src/main.rs, src/lib.rs (tauri::Builder default run), tauri.conf.json (v2
  schema, static frontendDist ../dist, single "main" window 1200x800),
  capabilities/default.json (core:default), generated RGBA icons/icon.png.
- Minimal static frontend at dist/index.html (no bundler; Tauri serves the
  static dir, so no npm build step needed for the scaffold).
- .gitignore for /target and /src-tauri/gen. Cargo.lock committed (app).

Verification:
- `cargo build --workspace` — Finished, compiles all 5 crates + Tauri app.
- `cargo test --workspace` — ok (0 tests, expected for scaffold).
- `cargo clippy --workspace` — clean, no warnings.

Env note: the outer sandbox makes ~/.cargo/registry unwritable (EPERM). Builds
here require CARGO_HOME pointed at a writable dir, e.g.
  CARGO_HOME=/tmp/loopfleet-cargo cargo build --workspace

2026-07-06 — M0: SQLite store with migrations for the data model
- Added rusqlite 0.32 (feature "bundled" — vendors SQLite, no system dep) to the
  workspace deps and the loopfleet-store crate.
- crates/store/src/migrations/0001_init.sql: full initial schema matching the PRD
  data model — projects, plans, tasks (PK = plan_id + anchor normalized_text,
  line_hint as tiebreaker), runs (Model B: plan_id + task_anchor FK to tasks),
  iterations, sessions (M5 schema defined now), events (single append-only log,
  AUTOINCREMENT seq). Derived TaskStatus is intentionally NOT stored. Indexes on
  events(owner,seq), runs(plan_id,task_anchor), plans(project_id).
- crates/store/src/lib.rs: migration runner. MIGRATIONS is an ordered
  (name, include_str! sql) list; migrate() creates schema_migrations, applies
  each pending migration in its own BEGIN/COMMIT with ROLLBACK on error, skips
  already-applied ones (idempotent). open(path) opens a connection, enables
  foreign_keys pragma, and migrates. ":memory:" supported for tests.
- Tests (4, all passing): full schema present after open, migrate idempotent,
  foreign keys enforced (plan -> missing project rejected), events.seq
  autoincrements.

Verification:
- CARGO_HOME=/tmp/loopfleet-cargo cargo test -p loopfleet-store — 4 passed.
- cargo clippy -p loopfleet-store — clean.
- cargo build --workspace — Finished (store + Tauri app compile).

Design notes:
- Migrations are append-only and keyed by name; never edit or reorder a shipped
  entry. Next migration is 0002_*.sql appended to MIGRATIONS.
- foreign_keys pragma is per-connection, so open() sets it every time; migrate()
  alone does not (callers using migrate() directly must set the pragma).

2026-07-06 — M0: Project registration (pick a folder, validate git repo, persist)
- crates/store/src/projects.rs: Project { id, repo_path, plan_convention }
  (serde-derived). insert_project (errors on the repo_path UNIQUE constraint) and
  list_projects (ordered by repo_path). Re-exported from store's lib.rs. Added
  serde to the store crate; fixed a duplicate `Connection` import in lib.rs
  (merged `use` + `pub use rusqlite::Connection` into one pub re-export).
- crates/gitx/src/lib.rs: is_git_repo(path) — read-only git2 Repository::open,
  recognizes work-dir and bare repos, never mutates. First real gitx code (rest
  of the git actor is M2). Added git2 dep + tempfile dev-dep.
- crates/core/src/lib.rs: register_project(conn, folder) ties it together —
  canonicalizes the path (so one repo can't register twice via different relative
  paths), validates via gitx::is_git_repo, generates a uuid v4 id, defaults
  plan_convention to "prd" (folder-convention detection is M3), inserts.
  RegisterError { NotAGitRepo, AlreadyRegistered, Store } with Display; the
  UNIQUE violation from the store maps to AlreadyRegistered. Added gitx/store/
  rusqlite/uuid deps + git2/tempfile dev-deps.
- src-tauri/src/lib.rs: AppState { db: Mutex<Connection> } opened in setup() at
  app_data_dir/loopfleet.db (single-writer; mutex because commands run on
  arbitrary threads). Tauri commands register_project(path) and list_projects,
  errors surfaced as strings. Wired the tauri-plugin-dialog for the native
  folder picker; enabled dialog:default capability and withGlobalTauri.
- dist/index.html: minimal UI — "Add project…" opens the native directory
  picker, invokes register_project, shows ok/err status, and refreshes a list of
  registered projects (repo_path + convention). Replaces the scaffold placeholder.
- Workspace deps added: git2 0.19, uuid 1 (v4), serde 1 (derive).

Verification:
- CARGO_HOME=/tmp/loopfleet-cargo cargo test -p loopfleet-core -p loopfleet-gitx
  -p loopfleet-store — 12 passed (core 3, gitx 3, store 6).
- cargo clippy --workspace --all-targets — clean, Tauri app included, 0 warnings.

Design notes:
- Path canonicalization is the dedup key; it also doubles as an existence check
  (a path that can't canonicalize can't be a repo → NotAGitRepo).
- The store stays git-agnostic (raw UNIQUE error); core owns the mapping to a
  user-facing AlreadyRegistered. Keeps the sandbox/git boundary out of store.

2026-07-06 — M1: Normalized event enum (adapter-sourced vs app-sourced lanes)
- crates/core/src/event.rs: NormalizedEvent, the single vocabulary every
  downstream consumer speaks. Variants match the PRD exactly: adapter-sourced
  lane (TurnStarted, AssistantText { text }, Reasoning { text }, ToolCall
  { call_id, name, input_excerpt }, ToolResult { call_id, ok, output_excerpt },
  CommandRun { cmd, exit: Option<i32> }, TurnCompleted { usage }, NeedsApproval,
  Failed { reason }, Ended) and the app-sourced lane (FileChanged { path }).
  ToolCall/ToolResult correlate by call_id (a shared String field on both).
- Lane enum { Adapter, App } + NormalizedEvent::lane(): only FileChanged is
  App; everything else is Adapter. The lane distinction is expressed as a method
  rather than splitting the type, so downstream still consumes one enum.
- Usage { input_tokens, output_tokens }: u64, Default, unreported counts = 0.
- Serde: internally tagged `#[serde(tag = "kind", rename_all = "snake_case")]`,
  so the store's normalized_event_json column holds e.g.
  {"kind":"tool_call","call_id":"…",…} and unit variants {"kind":"ended"}.
  All variants are unit or struct (no tuple variants), which internal tagging
  requires.
- NeedsApproval kept fieldless per PRD (only fires in M5 interactive sessions;
  headless never surfaces it — the sandbox is the boundary). No speculative
  fields added; revisit if M5 needs correlation.
- Placement: core, per core/lib.rs's own doc comment ("the normalized event
  enum" lands here). Re-exported from lib.rs (Lane, NormalizedEvent, Usage).
  store stays agnostic — it persists opaque JSON text, not the Rust type.
- Added serde dep to core, serde_json to core dev-deps + workspace deps
  (adapters will need serde_json for their stream mapping in the next M1 tasks).
- Tests (4, all passing): round_trips_every_variant (every variant survives
  JSON round-trip), kind_tag_shape_is_stable (the snake_case discriminator),
  tool_call_and_result_correlate_by_call_id, lanes_classify_correctly.

Verification:
- CARGO_HOME=/tmp/loopfleet-cargo cargo test -p loopfleet-core — 7 passed
  (4 event + 3 project registration).
- cargo clippy -p loopfleet-core --all-targets — clean.
- cargo build --workspace — Finished (Tauri app included).

Design notes:
- This enum is the most load-bearing decision in the app; changing it later
  touches everything. Get additions right the first time.
- CommandRun.exit is Option<i32>: a killed command (SIGTERM at an iteration
  boundary) produces no exit code.

2026-07-06 — M1: AgentAdapter trait + stub replay adapter
- crates/adapters/src/lib.rs: the AgentAdapter trait, object-safe via async_trait
  so the M3 supervisor can hold a Box<dyn AgentAdapter> chosen by agent name.
  start_run(&RunSpec) -> RunHandle; open_session(&Path, SessionSeed) ->
  SessionHandle stays in the frozen signature but v1 impls return
  AdapterError::SessionsUnsupported (interactive sessions are M5).
  - RunSpec { cwd, prompt }: only what the stub + supervisor need in M1; per-agent
    flag sets / model land as real adapters do. SessionSeed { plan_file } present
    so the signature is frozen now, consumed by no v1 adapter.
  - RunHandle / SessionHandle wrap a tokio mpsc::Receiver<NormalizedEvent>. Bounded
    channel = the backpressure contract (matches the later event-log-writer task):
    a slow consumer stalls the producer, no unbounded buffer. Process-group
    ownership + stop/SIGTERM stay with the M3 supervisor, not the handle.
  - AdapterError { Spawn(io), Protocol(String), SessionsUnsupported } + Display /
    Error. Derived Debug on the handles so tests can unwrap_err.
- crates/adapters/src/stub.rs: StubAdapter — replays a canned event log, no
  process/network, deterministic. This is the fixture all UI work builds against
  before a real adapter exists. new(Vec<NormalizedEvent>) or from_jsonl(&str)
  (one event per non-empty line, same {"kind":…} shape the store persists; blank
  lines skipped, malformed JSON is a hard error). start_run clones the events and
  tokio::spawns a task that sends them in order over the channel, stopping if the
  receiver is dropped. open_session returns SessionsUnsupported.
- crates/adapters/fixtures/basic_run.jsonl: a realistic 9-event run (turn_started
  → assistant_text/reasoning → correlated tool_call/tool_result → command_run →
  turn_completed → ended) for the from_jsonl loader and UI reuse.
- Deps: added tokio (rt, sync, macros) and async-trait to workspace deps; adapters
  now depends on loopfleet-core, async-trait, tokio, serde_json. First async +
  tokio in the tree — foundational for the supervisor/event-log work, not
  speculative (PRD's trait is async; Tauri v2 is tokio-based).

Verification:
- CARGO_HOME=/tmp/loopfleet-cargo cargo test -p loopfleet-adapters — 5 passed
  (replay order, run independence, jsonl fixture load+correlate, blank/bad-json
  handling, open_session unsupported).
- cargo clippy -p loopfleet-adapters --all-targets — clean.
- cargo build --workspace — Finished (Tauri app included).

Design notes:
- Object safety drove async-trait over native async-fn-in-trait: the supervisor
  dispatches adapters dynamically by agent name, which needs dyn.
- The stub streams through the exact RunHandle path a real run uses, so UI and
  the event-log writer can be built and tested with zero agent binaries present.

2026-07-06 — M1: Claude Code adapter, headless
- Captured ground truth first: ran the real `claude 2.1.201` binary with
  `-p … --output-format stream-json --verbose --dangerously-skip-permissions`
  in a throwaway git repo to see the actual line shapes, rather than mapping from
  memory. Line types observed: system/init, assistant (content blocks: thinking,
  text, tool_use), user (tool_result), result/success, plus noise (system
  hook_started/response, rate_limit_event).
- crates/adapters/src/claude.rs: ClaudeAdapter (stateless) + ClaudeMapper
  (stateful over one run's stream).
  - start_run spawns `claude` in spec.cwd, stdin null, stdout/stderr piped, and
    tokio::spawns drive() which reads stdout line-by-line, maps each via
    ClaudeMapper, and forwards over the same bounded mpsc(64) RunHandle path the
    stub uses (backpressure contract preserved). Receiver-dropped → start_kill +
    stop. Stream ending without a terminal `result` → synthesize
    Failed{reason: stderr} + Ended so consumers always see termination.
  - Mapping: system/init → TurnStarted. assistant blocks: text → AssistantText
    (empty skipped), thinking → Reasoning (empty skipped), tool_use → CommandRun
    when name=="Bash" (cmd from input.command, exit:None — Claude's bash result
    carries no numeric exit code) else ToolCall{call_id=id, name, input_excerpt}.
    user tool_result → ToolResult{call_id=tool_use_id, ok=!is_error,
    output_excerpt}, EXCEPT results of Bash calls are dropped (already normalized
    to CommandRun, which the enum carries with no result pairing — mapper tracks
    bash_calls ids). result → TurnCompleted{usage:{input_tokens,output_tokens}}
    on success or Failed{reason:result} on error, always followed by Ended.
    Unknown line types + blanks → ignored.
  - Excerpts truncated to 2000 bytes on a char boundary (event log stores
    excerpts, not full payloads). tool_result content flattened whether it's a
    plain string or an array of text blocks.
- crates/adapters/fixtures/claude_stream.jsonl: a 9-line fixture distilled from
  the real capture (init trimmed to the fields the mapper reads; real toolu_ ids,
  real Read/Bash tool_use + results, real usage numbers, a rate_limit_event noise
  line). The mapper's primary test runs against this.
- Cargo.toml: adapters tokio now enables process + io-util; dev-deps add
  tokio rt-multi-thread + tempfile "3" for the live test.
- Tests (10 mapper/unit + 1 ignored live): captured-stream mapping (asserts the
  full expected sequence, Read pair correlates, Bash→CommandRun with its result
  dropped, exactly one ToolResult, usage parsed), thinking→Reasoning &
  text→AssistantText, empty blocks skipped, non-Bash tool_use excerpt, error
  tool_result → ok:false, error result → Failed+Ended, noise/blank ignored,
  invalid JSON → Protocol error, array content flattened, excerpt char-boundary
  truncation. Plus live_run_against_fixture_repo (#[ignore]) which spawns the
  real CLI in a temp git repo — exercises spawn+drive end to end.

Verification:
- CARGO_HOME=/tmp/loopfleet-cargo cargo test -p loopfleet-adapters — 15 passed,
  1 ignored.
- cargo test -p loopfleet-adapters -- --ignored live_run — 1 passed (real
  claude 2.1.201, ~10s, drove spawn→stream→Ended, saw TurnCompleted).
- cargo clippy -p loopfleet-adapters --all-targets — clean.
- cargo build --workspace — Finished (Tauri app included).

Design notes:
- CommandRun for Bash carries exit:None on purpose: Claude's bash tool_result
  exposes is_error (bool) but no numeric exit code, and the enum's exit is the
  process exit code — fabricating Some(0)/Some(1) would misrepresent it. The
  bash output itself is not representable (CommandRun has no output field); that
  is the PRD's normalization, and FileChanged (app lane) will catch effects.
- The mapper is stateful (bash_calls set) and fed one line at a time, so drive()
  stays a thin read-loop and the mapping is unit-testable with zero process.

Next highest-priority task: M1 — pi adapter, headless: spawn `pi --mode json`,
map AgentEvent JSONL to the enum, integration test. (Or the event-log writer
task, pickable independently.)

2026-07-06 — M1: pi adapter, headless
- Captured ground truth first: ran the real `pi 0.80.3` binary with
  `-p --mode json` in a throwaway git repo to see the actual line shapes. The
  default (google) provider succeeded; the anthropic route was billing-blocked,
  but that capture still confirmed the error-envelope shape (turn_end.message
  with stopReason:"error" + errorMessage). Line types observed: session,
  agent_start, turn_start, message_start/message_update(delta)/message_end,
  tool_execution_start/update/end, turn_end, agent_end.
- crates/adapters/src/pi.rs: PiAdapter (stateless) + PiMapper (stateful over one
  run's stream). Same structure as ClaudeAdapter (drive() read-loop, bounded
  mpsc(64) RunHandle, receiver-drop → start_kill, EOF-without-terminal →
  synthesize Failed+Ended).
  - start_run spawns `pi -p --mode json <prompt>` in spec.cwd, stdin null,
    stdout/stderr piped. No per-agent permission-bypass flag: in headless `-p`
    mode pi auto-resolves permission prompts (PRD sandbox note), and the M2
    Seatbelt profile is the real boundary. The live test confirmed the bash tool
    executes without hanging.
  - Mapping (source of truth = the *complete* events, streaming deltas are
    noise): turn_start → TurnStarted. message_end (role=="assistant" only) →
    per content block: text → AssistantText, thinking → Reasoning (both skip
    empty); toolCall blocks are NOT mapped here (tool activity comes from
    tool_execution_*, so mapping them here would double-emit). Non-assistant
    message_end (the echoed user prompt, toolResult messages) → ignored.
    tool_execution_start → CommandRun{cmd,exit:None} when toolName=="bash" (id
    recorded) else ToolCall{call_id=toolCallId,name=toolName,input_excerpt=args}.
    tool_execution_end → ToolResult{call_id,ok=!isError,output_excerpt} EXCEPT
    bash ends are dropped (already CommandRun). turn_end →
    TurnCompleted{usage:{input,output}} or Failed{reason:errorMessage} when the
    turn's stopReason=="error". agent_end → Ended. session/agent_start/
    message_start/message_update/tool_execution_update + unknown/blank → ignored.
  - A pi headless run has multiple agent turns, so it emits multiple
    TurnStarted/TurnCompleted pairs (per-turn usage) before the single Ended —
    unlike Claude's single-result headless shape. Downstream consumes the enum
    turn-count-agnostically.
  - Excerpts truncated to 2000 bytes on a char boundary; tool result content
    flattened whether string or array of text blocks. Helpers (excerpt/compact/
    stringify_content/parse_usage/read_stderr) are file-local, mirroring
    claude.rs's self-contained structure (each adapter owns its formatting).
- crates/adapters/fixtures/pi_stream.jsonl: 15-line fixture distilled from the
  real capture (session/agent_start/turn_start noise, a user-prompt echo and a
  message_update delta to exercise skipping, the assistant thinking+toolCall
  message, real tool_execution start/end pairs for read+bash, both turn_ends
  with real usage, agent_end).
- Cargo.toml: no new deps (adapters already had tokio process/io-util,
  serde_json, async-trait; dev-deps tempfile + tokio rt-multi-thread from the
  Claude adapter).
- Tests (13 mapper/unit + 1 ignored live): captured-stream mapping (two turns
  with per-turn usage, Reasoning first, read pair correlates, bash→CommandRun
  with its end dropped, exactly one ToolResult, two TurnStarted), thinking→
  Reasoning & text→AssistantText, non-assistant message_end ignored, empty
  blocks skipped, toolCall-in-message not mapped, non-bash tool_start excerpt,
  bash CommandRun + end dropped, error tool_end → ok:false, errored turn →
  Failed, agent_end → Ended, noise/blank ignored, invalid JSON → Protocol error,
  excerpt char-boundary truncation. Plus live_run_against_fixture_repo (#[ignore])
  spawning the real CLI.

Verification:
- CARGO_HOME=/tmp/loopfleet-cargo cargo test -p loopfleet-adapters — 28 passed,
  2 ignored.
- cargo test -p loopfleet-adapters -- --ignored pi::tests::live_run — 1 passed
  (real pi 0.80.3, ~4s, drove spawn→stream→Ended, saw TurnCompleted).
- cargo clippy -p loopfleet-adapters --all-targets — clean.
- cargo build --workspace — Finished (Tauri app included).

Design notes:
- CommandRun for bash carries exit:None on purpose: pi's bash tool_execution_end
  exposes isError (bool) but no numeric exit code, matching the Claude adapter's
  decision and the enum's process-exit semantics.
- Tools are sourced from tool_execution_start/end rather than the assistant
  message's toolCall blocks: the execution events carry the results and isError
  in one place, and using the message blocks too would double-emit calls.

Next highest-priority task: M1 — cursor-agent adapter, headless: spawn
`cursor-agent -p --output-format stream-json`, map its stream to the enum,
integration test. (Or the event-log writer task, pickable independently.)

2026-07-06 — M1: cursor-agent adapter, headless
- Captured ground truth first: ran the real `cursor-agent 2026.07.01` binary with
  `-p --output-format stream-json --force` in a throwaway git repo. Line types
  observed: system/init, user (echoed prompt), assistant (content blocks: text
  only — no reasoning surfaced even on a High reasoning model), tool_call with
  subtype started/completed, result/success. cursor-agent emits one JSON per
  newline-terminated line; embedded newlines inside string fields (notably
  call_id) are JSON-escaped as \n, so line splitting stays safe.
- crates/adapters/src/cursor.rs: CursorAdapter (stateless) + CursorMapper (also
  stateless — every tool_call event carries the full typed tool_call object, so
  shell vs non-shell is decided per line with no cross-line state, unlike the pi
  mapper's bash-id set). Same drive() read-loop / bounded mpsc(64) RunHandle /
  receiver-drop→start_kill / EOF-without-terminal→synthesize Failed+Ended shape
  as the claude and pi adapters.
  - start_run spawns `cursor-agent -p --output-format stream-json --force
    <prompt>` in spec.cwd, stdin null, stdout/stderr piped. --force bypasses
    cursor-agent's own permission prompts; in headless it fabricates a "user
    skipped" answer anyway (PRD), so the M2 Seatbelt profile is the real
    boundary — --force just keeps the run from stalling. Live test confirmed the
    shell tool executes without hanging.
  - Mapping: system/init → TurnStarted. assistant message content: text →
    AssistantText (empty skipped). cursor-agent's stream-json does NOT surface
    reasoning, so no Reasoning is emitted (documented, not guessed — only text
    blocks were observed). user echo + unknown lines → ignored. tool_call is
    keyed by tool kind (readToolCall, shellToolCall, …); tool_entry() finds the
    single key ending in "ToolCall" and strips the suffix for a normalized name
    (read, shell, …). subtype started: non-shell → ToolCall{call_id,name,
    input_excerpt=args}; shell → nothing (waits for completed). subtype
    completed: non-shell → ToolResult{call_id, ok=(result has "success" key),
    output_excerpt}; shell → CommandRun{cmd, exit}. result → TurnCompleted
    {usage:{inputTokens,outputTokens}} on success or Failed{reason} on is_error,
    always followed by Ended.
  - KEY DIFFERENCE from claude/pi: cursor-agent reports a real numeric exitCode
    for shell commands (tool_call.shellToolCall.result.success.exitCode), so
    CommandRun.exit is populated with Some(code) — the enum's Option<i32> exists
    to carry it when an agent provides it, and cursor is the one that does.
    Shell's `started` event is dropped (no exit code yet) and CommandRun is
    emitted at `completed` instead; when no success/exitCode is present (an error
    variant) exit falls back to None.
  - Excerpts truncated to 2000 bytes on a char boundary; a non-shell tool
    result's output prefers a plain `content` string, else compact JSON of the
    success/error object. Helpers (excerpt/compact/result_excerpt/parse_usage/
    subtype/read_stderr) file-local, mirroring the self-contained claude.rs/pi.rs.
- crates/adapters/fixtures/cursor_stream.jsonl: 9-line fixture generated
  programmatically from the real capture (trimming the very large shellToolCall
  args to command+workingDirectory, read result to content+metadata) so the
  escaped call_id (with its embedded \n) is byte-exact. system/init, user echo,
  assistant text, read start/complete pair, shell start/complete pair, final
  "done" text, result with real usage.
- lib.rs: registered `mod cursor` + `pub use cursor::CursorAdapter`.
- Tests (14 mapper/unit + 1 ignored live): captured-stream mapping (asserts
  TurnStarted/Ended bracket, read pair correlates despite the newline in the id,
  shell→CommandRun with exit Some(0), exactly one ToolResult, usage parsed),
  system init→TurnStarted, non-init system ignored, assistant text + empty
  skipped, user ignored, non-shell start→ToolCall excerpt, non-shell complete→
  ToolResult, error-variant result→ok:false, shell start dropped + complete→
  CommandRun with exit, shell without exitCode→None, result success→
  TurnCompleted+Ended, result error→Failed+Ended, invalid JSON→Protocol error,
  excerpt char-boundary truncation. Plus live_run_against_fixture_repo (#[ignore])
  spawning the real CLI.

Verification:
- CARGO_HOME=/tmp/loopfleet-cargo cargo test -p loopfleet-adapters — 42 passed,
  3 ignored.
- cargo test -p loopfleet-adapters -- --ignored cursor::tests::live_run — 1
  passed (real cursor-agent 2026.07.01, ~9s, drove spawn→stream→Ended, saw
  TurnCompleted with real usage).
- cargo clippy -p loopfleet-adapters --all-targets — clean.
- cargo build --workspace — Finished (Tauri app included).

Design notes:
- The mapper is stateless because both the started and completed tool_call events
  carry the fully-typed tool_call object; shell detection is a per-line key check
  (shellToolCall present), so no id set is needed — a small simplification over
  the pi adapter's bash_calls set.
- CommandRun.exit is the one place across the three v1 adapters where a real exit
  code lands: claude and pi expose only an isError bool, cursor exposes exitCode.
  Downstream (timeline) already handles Option<i32>, so mixed None/Some across
  agents is expected and fine.

Next highest-priority task: M1 — event-log writer: single-writer SQLite via a
bounded channel (the backpressure); FileChanged emitted here from worktree
watching, not the adapters. (This is now the only remaining M1 task — all three
v1.0 headless adapters are done.)

2026-07-06 — M1: Event-log writer (single-writer SQLite via a bounded channel)
- crates/store/src/event_log.rs: EventLog — the one place rows are appended to
  the `events` table. spawn(conn, capacity) takes ownership of the Connection and
  drains a bounded tokio mpsc of LogEntry on a tokio::task::spawn_blocking loop
  (rx.blocking_recv()), inserting each row. SQLite is single-writer, so funneling
  every producer through one writer means concurrent runs never contend for the
  write lock. The bounded channel IS the backpressure: a slow/bursty writer
  stalls producers at send().await instead of growing an unbounded buffer
  (matches the RunHandle mpsc(64) contract the adapters already use).
  - spawn_blocking (not a plain async task): keeps the synchronous rusqlite calls
    off the async runtime's worker threads; the writer returns
    rusqlite::Result<usize> (events written) so a write error stops the drain and
    surfaces on shutdown().await.
  - sender() -> clonable Sender (re-exported as EventSender): one clone per
    producer — a per-run adapter-forwarding task (M3) plus the app's worktree
    watcher (M2/M3). Writer stops when the last sender drops. shutdown() drops
    the log's own sender, awaits the writer, returns the count.
  - KEY BOUNDARY: the store stays AGNOSTIC to the event type. LogEntry carries
    { owner_id, event_json: String } — the already-encoded {"kind":…} JSON that
    NormalizedEvent serializes to — NOT the core enum. This is deliberate: core
    depends on store (register_project), so store depending on core would be a
    build cycle (cargo rejected it), and the enum's own design note says "store
    persists opaque JSON text, not the Rust type." Producers serialize before
    sending; consumers (load_events) get the JSON back and deserialize. So this
    task touched zero core code.
  - FileChanged (app-sourced lane) enters through the SAME Sender as
    adapter-sourced events — the channel is the merge point and imposes the
    ordering. Adapters never emit FileChanged; the app pushes it. The fs/git
    watcher that generates it needs worktrees, so it lands with M2/M3; M1
    establishes only that the app lane is a first-class writer input (proven by
    a test that mixes a TurnStarted from an "adapter" sender and a FileChanged
    from an "app" sender into one owner's log).
  - load_events(conn, owner_id) -> Vec<StoredEvent{seq,event_json,ts}>: reads a
    run/session's events back in append (seq) order via a separate connection.
    Used by the M4 timeline and by the tests here. Reads stay off the writer.
  - now_millis(): events.ts is unix millis, stamped at write time.
- crates/store/Cargo.toml: added tokio (rt, sync) as a dep; dev-deps tokio
  (rt-multi-thread, macros) + tempfile "3" for the file-backed round-trip tests.
  No serde_json / loopfleet-core dep — store never (de)serializes the enum.
- crates/store/src/lib.rs: mod event_log + re-exports (EventLog, LogEntry,
  EventSender, StoredEvent, load_events). Renamed the re-export of `Sender` to
  `EventSender` so the crate root name is unambiguous.
- Tests (3, all passing): round_trips_through_sqlite (events written by the
  writer connection read back byte-for-byte and in strictly increasing seq order
  through a separate reader connection), file_changed_flows_through_the_same_
  channel (adapter-lane + app-lane events from two senders land in one owner's
  log, in order), separates_events_by_owner (concurrent runs share one writer,
  stay separable by run_or_session_id).

Verification:
- CARGO_HOME=/tmp/loopfleet-cargo cargo test -p loopfleet-store — 9 passed
  (3 event-log + 6 pre-existing store/projects).
- cargo clippy -p loopfleet-store --all-targets — clean.
- cargo build --workspace — Finished (Tauri app included).

Design notes:
- The writer is intentionally event-type-agnostic: serialization is a boundary
  concern that belongs above the store, at the point that owns NormalizedEvent
  (the M3 supervisor's forwarding task). Keeping it out of store preserves the
  core→store dependency direction and keeps the store a pure persistence layer.
- spawn_blocking + blocking_recv is the single-writer thread model: one owned
  Connection, one drain loop, no locks, no async-blocking-on-sync footgun.

M1 is COMPLETE — normalized enum, adapter trait + stub, all three v1.0 headless
adapters (Claude Code, pi, cursor-agent), and the event-log writer are done.
Next milestone: M2 — Sandbox and git (port ralph.sb to a template; Sandbox
trait + SeatbeltSandbox; parent-.git commit regression test; worktree manager;
shadow-ref snapshotter; diff service).

2026-07-06 — M2: Port ralph.sb to a per-run template + renderer
- crates/sandbox/src/profile.sb.tmpl: the Seatbelt (SBPL) profile, rendered per
  run. (version 1) + (deny default), then the minimum a full-auto headless agent
  needs re-granted: file-read* / file-read-metadata open (agents read from
  everywhere — stated plainly, read-scope confinement is post-v1), process-exec/
  fork + signal(self), sysctl-read/mach-lookup/ipc-posix-shm (toolchain startup),
  network* open (remote inference + tool egress; egress allowlist is post-v1), a
  fixed set of writable device nodes (/dev/null, tty, std{out,err}, {u}random,
  /dev/fd, …), and a parameterized (allow file-write* {{WRITE_SUBPATHS}}) block —
  the actual boundary.
- crates/sandbox/src/lib.rs: render(&RenderParams) -> Result<String, RenderError>.
  RenderParams { worktree, progress_dir, agent_dirs[], temp_dirs[] }; ::new sets
  the standard macOS temp set (default_temp_dirs: /tmp, /private/tmp, /var/folders,
  /private/var/folders — Seatbelt matches resolved paths, so grant both symlink
  and target) and empty agent_dirs. render assembles worktree + progress_dir +
  agent_dirs + temp_dirs into (subpath "…") lines and splices them into the
  template's marker.
  - SECURITY: each write path is validated absolute (RelativePath error — a
    relative path in the boundary is a bug) and UTF-8 (NonUtf8Path), then escaped
    (escape_sbpl: backslash then quote) before entering the SBPL string literal.
    The profile IS the boundary, so a path with " or \ must not break out of its
    literal. Tested with a /repo/a"b\c path.
- DECISION (resolves a PRD contradiction): the parent repo's .git is NOT granted
  write. The Sandbox design section (PRD lines 83/86/92) is emphatic and reasoned
  — commits are app-owned (the unsandboxed git actor snapshots to shadow refs),
  the agent never runs git commit, and .git write is a real escape (planted
  hooks/ script, core.hooksPath / core.sshCommand in config that later runs
  unsandboxed). The M2 milestone bullets still said "parent .git path" + a
  ".git-grant" regression test; that wording is stale and superseded. Updated
  the PRD: this task's line drops the .git param and notes the supersession; the
  3rd M2 task (the .git-grant regression test) is flagged REVISIT — its premise
  no longer holds, likely becomes "agent CANNOT write parent .git while the app's
  out-of-sandbox actor still commits". No .git param exists in the renderer.
- Cargo.toml: dev-dep tempfile "3" (for the sandbox-exec parse test only). No
  runtime deps — pure string rendering, no templating crate (simplicity).
- Tests (7 unit + 1 ignored): all four writable subpaths render; no marker left;
  boundary shape preserved (deny default / file-read* / network* / file-write*);
  no .git in any (subpath …) grant for default params; relative path rejected;
  quotes+backslashes escaped; default_temp_dirs covers the /private paths. Plus
  parses_under_sandbox_exec (#[ignore], cfg macos): writes the rendered profile
  and runs `sandbox-exec -f run.sb /usr/bin/true` — proves it is valid SBPL. It
  tolerates the nested-sandbox "sandbox_apply: Operation not permitted" (the
  profile parsed; apply is denied because the test itself runs inside the outer
  build sandbox) while still failing on a real parse error.

Verification:
- CARGO_HOME=/tmp/loopfleet-cargo cargo test -p loopfleet-sandbox — 7 passed,
  1 ignored.
- cargo test -p loopfleet-sandbox -- --ignored parses_under_sandbox_exec — 1
  passed (real /usr/bin/sandbox-exec parsed the rendered profile).
- cargo clippy -p loopfleet-sandbox --all-targets — clean.
- cargo build --workspace — Finished (Tauri app included).

Design notes:
- Write subpaths are generated in Rust (variable-length agent_dirs/temp_dirs)
  rather than fixed template placeholders, so the profile parameterizes cleanly
  over any agent's config/cache dirs.
- The template embeds the security rationale in comments (why reads/network are
  open, why .git write is excluded) so the rendered .sb shown in the run UI's
  profile panel is self-documenting — trust is a feature, not a footnote.

Next highest-priority task: M2 — Sandbox trait + SeatbeltSandbox impl wrapping
command construction (wrap a command in `sandbox-exec -f <rendered.sb> …`).

2026-07-06 — M2: Sandbox trait + SeatbeltSandbox impl (command construction)
- crates/sandbox/src/lib.rs: the Sandbox trait, kept behind a trait so non-macOS
  backends (Landlock/bubblewrap) slot in later without leaking Seatbelt specifics
  upward (PRD "Sandbox"). Single method:
    fn wrap(&self, command: &SandboxCommand) -> Result<WrappedCommand, SandboxError>;
  Object-safe (&self, owned return) so the M3 supervisor can hold a Box<dyn
  Sandbox>. No async — pure render + file write + argv construction.
  - SandboxCommand { program: OsString, args: Vec<OsString>, params: RenderParams,
    profile_path: PathBuf }. profile_path is app-owned, keyed by run-id, set by
    the supervisor (the sandbox doesn't know about run ids). params is the
    existing per-run RenderParams (write boundary).
  - WrappedCommand { program (private OsString), args (private Vec<OsString>),
    profile_path (pub) } with program()/args() accessors. Deliberately OPAQUE:
    the caller spawns program()+args() as-is and never learns it's sandbox-exec
    — that's how Seatbelt stays behind the trait. profile_path is surfaced for
    the run UI's profile panel (trust is a feature, not a footnote). Derives
    Debug so tests can unwrap_err.
  - SeatbeltSandbox (unit struct, stateless). wrap(): render(&params) → create
    profile_path.parent() dir → write the .sb → build argv
    ["-f", profile_path, program, ...args] with program=SANDBOX_EXEC
    ("/usr/bin/sandbox-exec"). Each wrap renders a fresh profile for the run it
    confines. cwd/env stay the caller's concern (adapters already set
    .current_dir on their tokio Command; sandbox-exec inherits cwd), so wrap
    only produces the argv — the adapter/supervisor builds whatever Command type
    (std or tokio) from program()+args().
  - SandboxError { Render(RenderError), Io(io::Error) } + Display/Error + From<
    RenderError> so render()'s error flows through `?`. Render errors (relative/
    non-UTF-8 write path) propagate unchanged; disk write failures become Io.
- No new deps: pure std (OsString/OsStr, fs, process in the ignored test).
  tempfile dev-dep already present.
- Tests (4 unit + 1 ignored macos, on top of the 7 render tests): argv built in
  exact order (sandbox-exec, -f, profile, program, args); profile written to
  disk contains (deny default) + the worktree subpath; parent dir created for a
  nested profile_path; render errors propagate as Render(RelativePath). Plus
  wrapped_command_spawns (#[ignore], macos): wrap /usr/bin/true and actually
  spawn the returned argv, tolerating nested sandbox_apply denial — proves the
  sandbox-exec -f <profile> <program> shape is spawnable end to end.

Verification:
- CARGO_HOME=/tmp/loopfleet-cargo cargo test -p loopfleet-sandbox — 11 passed,
  2 ignored.
- cargo test -p loopfleet-sandbox -- --ignored wrapped_command_spawns — 1 passed
  (real /usr/bin/sandbox-exec ran the wrapped /usr/bin/true).
- cargo clippy -p loopfleet-sandbox --all-targets — clean.
- cargo build --workspace — Finished (Tauri app included).

Design notes:
- WrappedCommand is opaque by construction (private program/args + accessors),
  not by convention: nothing above the Sandbox trait can pattern-match out the
  sandbox-exec binary, which keeps the PRD's "no Seatbelt details leaking above
  the trait" honest.
- The sandbox writes the profile itself (not the caller) so the profile-on-disk
  and the argv referencing it are produced atomically in one call — a caller
  can't accidentally spawn against a stale/missing .sb.

Next highest-priority task: M2 — Worktree manager (create/remove via git CLI,
branch naming, orphan cleanup on startup). NB: the 3rd M2 bullet (the
".git-grant" regression test) is flagged REVISIT/stale — its premise (parent
.git write grant) was removed; it likely becomes "agent CANNOT write parent
.git while the app's out-of-sandbox actor still commits", better done once the
worktree manager + shadow snapshotter exist.

2026-07-06 — M2: Worktree manager (create/remove via git CLI, orphan cleanup)
- crates/gitx/src/worktree.rs: per-run worktree lifecycle, shelling out to the
  `git` CLI (libgit2 worktree support is patchy; agents expect CLI semantics —
  PRD "Git layer"). git2 stays for reads; this module owns only the mutating
  worktree ops. Kept actor-agnostic: it builds/runs the commands, the M3
  supervisor funnels them through the single serialized git actor later.
  - BRANCH_PREFIX = "agent/" + branch_for(run_id) -> "agent/<run-id>" (PRD
    branch naming). Worktree { path, branch }.
  - add(repo, worktrees_root, run_id): `git -C <repo> worktree add -b
    agent/<run-id> <worktrees_root>/<run-id>` (needs a HEAD to branch from).
    Creates worktrees_root if missing. Canonicalizes the returned path because
    git records the canonical form (macOS /var -> /private/var) and `list`
    reports it that way — so add()'s path matches list()'s and disk.
  - remove(repo, path): `git worktree remove --force <path>`. --force because
    agents leave dirty/untracked trees (commits are app-owned via shadow refs,
    so the checkout is expected dirty at teardown). The agent/<run-id> branch
    and shadow refs are intentionally left intact so the run's diff history
    survives (PRD "keep all shadow refs").
  - list(repo): parses `git worktree list --porcelain`, filtering to worktrees
    on an agent/ branch (strips refs/heads/), so the repo's own main worktree
    and unrelated ones don't appear.
  - cleanup_orphans(repo): startup orphan cleanup = `git worktree prune` (git's
    own mechanism for entries whose checkout dir vanished, e.g. a crash mid-run).
    Branches/refs untouched. Returns count pruned (before/after worktree count
    diff). Scoped to git's notion of orphan; run-record reconciliation (mark
    running runs failed on restart) stays M6.
  - WorktreeError { Io, Git(stderr) }, From<io::Error>, Display/Error. git()
    helper runs `git -C <repo> …`, returns trimmed stdout or stderr on non-zero.
- crates/gitx/src/lib.rs: `pub mod worktree` + re-export Worktree/WorktreeError.
- No new deps (std::process::Command; tempfile dev-dep already present).
- Tests (9 total, all passing): branch naming; add creates worktree on
  agent/<id> branch with populated checkout + shows in list; remove deletes a
  dirty+untracked worktree yet keeps the agent branch; list ignores non-agent
  (feature/x) worktrees; cleanup prunes an orphaned entry (dir deleted out from
  under git) → gone from list; cleanup is a no-op (0) when a live worktree
  exists. repo_with_commit() test helper inits a repo with one commit + user
  config so `worktree add -b` has a HEAD.

Verification:
- CARGO_HOME=/tmp/loopfleet-cargo cargo test -p loopfleet-gitx — 9 passed.
- cargo clippy -p loopfleet-gitx --all-targets — clean.
- cargo build --workspace — Finished (Tauri app included).

Design notes:
- Path canonicalization in add() is the fix for a real caller footgun: without
  it the stored path (symlinked /var/...) never equals what list()/git report
  (/private/var/...), so later remove()/lookup by list() would miss it.
- cleanup_orphans is deliberately narrow (git prune only). Removing live agent
  worktrees on restart because "runs don't survive app restart in v1" belongs
  with M6 crash-recovery + run-record reconciliation, not here.

Next highest-priority task: M2 — Shadow-ref snapshotter: commit worktree state
to refs/agentapp/run-<id>/iter-<n> after each iteration (app-owned commits, the
unsandboxed git actor; agent never runs git commit). Then the diff service, then
the flagged-stale ".git-grant" regression test can be rewritten as "agent CANNOT
write parent .git while the app's out-of-sandbox actor still commits".

2026-07-06 — M2: Shadow-ref snapshotter (app-owned commits to refs/agentapp/…)
- crates/gitx/src/shadow.rs: snapshot(repo, worktree, run_id, iter) -> Snapshot
  { git_ref, commit }. Captures the worktree's FULL state (tracked + untracked,
  respecting .gitignore) to refs/agentapp/run-<id>/iter-<n> after each iteration.
  App-owned commits — the agent never runs git commit and never gets .git write
  (PRD "Git layer"). Shells out to git (a mutation, consistent with worktree.rs;
  git2 stays for reads). Stays actor-agnostic; M3 funnels it through the one
  serialized git actor.
  - Throwaway index: GIT_INDEX_FILE points at a per-(pid,run,iter) temp file, so
    `git add -A` + `write-tree` snapshot disk exactly WITHOUT touching the
    worktree's own index — the agent's staged/unstaged state at teardown is
    preserved. Tree objects land in the shared object database.
  - .gitignore respected (via add -A) so build artifacts (target/, node_modules)
    are not snapshotted.
  - commit-tree runs under a FIXED app identity (loopfleet <loopfleet@localhost>
    via GIT_AUTHOR_*/GIT_COMMITTER_* env), not the repo's user.* config — the
    commit is the app's, and this never fails on a repo with no user config.
  - Parent chain: iter-n parents on iter-(n-1)'s shadow commit if it exists, else
    the worktree's base HEAD (agent branch tip; agent never commits, so this is
    the commit the worktree was cut from). Gives a readable `git log <shadow>`
    history. Diffs work between any two refs regardless, but the chain makes the
    "diffable history" real. update-ref only ever touches refs/agentapp/… — the
    agent branch and user branches are never advanced.
  - shadow_ref(run_id, iter) -> "refs/agentapp/run-<id>/iter-<n>" (public helper,
    the ref-naming convention in one place). Snapshot { git_ref, commit } returned
    for the store (Iteration.shadow_ref) and the M2 diff service.
  - SnapshotError { Io, Git(stderr) } + Display/Error/From<io::Error>, mirroring
    WorktreeError's shape (each gitx module owns its git-CLI error).
- crates/gitx/src/lib.rs: pub mod shadow + re-exports (shadow_ref, Snapshot,
  SnapshotError). lib.rs doc already anticipated shadow-ref ops in M2.
- No new deps (std::process::Command + std::env::temp_dir; tempfile dev-dep
  already present).
- Tests (6, all passing): shadow_ref naming; snapshot captures a tracked edit +
  an untracked file (tree matches disk via `git show <commit>:path`); first
  snapshot parents on base AND leaves the agent branch tip unchanged; iterations
  chain (iter-2 parent == iter-1 commit, name-only diff shows the second edit);
  snapshot leaves the worktree's real index untouched (status still ` M`/`??`);
  .gitignore respected (ignored file absent from the tree, kept file present).
  repo_with_worktree() helper inits a repo+commit and cuts a run worktree via
  crate::worktree::add.

Verification:
- CARGO_HOME=/tmp/loopfleet-cargo cargo test -p loopfleet-gitx — 15 passed
  (6 shadow + 9 pre-existing worktree/repo).
- cargo clippy -p loopfleet-gitx --all-targets — clean.
- cargo build --workspace — Finished (Tauri app included).

Design notes:
- The throwaway-index trick is the crux: it lets the app snapshot arbitrary
  worktree state (including untracked files) without ever mutating the worktree's
  index or HEAD, so the sandboxed agent's in-progress state is never perturbed by
  the app's out-of-band commits.
- Fixed synthetic identity (not repo user.*) keeps app-owned commits attributable
  and makes commit-tree robust on freshly-cut worktrees / unconfigured repos.

Next highest-priority task: M2 — Diff service: iteration diff, run cumulative
diff, run-vs-run diff (via git2). Then the flagged-stale ".git-grant" regression
test can be rewritten as "agent CANNOT write parent .git while the app's
out-of-sandbox actor still commits", now that the snapshotter proves the actor
commits out-of-sandbox.

2026-07-06 — M2: Diff service (iteration / cumulative / run-vs-run, via git2)
- crates/gitx/src/diff.rs: read-only diff service over the app-owned shadow refs.
  Uses git2 (reads stay on git2; mutations shell out — PRD "Git layer"), so it's
  concurrent and never funnels through the M3 git actor. The three UI views all
  reduce to diffing two tree-ish revisions, so the core is one private build()
  and the named views are thin ref-resolving wrappers (simplicity, no duplicated
  diff logic).
  - iteration_diff(repo, run_id, iter): diff of iter's shadow commit vs its first
    parent. The shadow refs form a chain (snapshotter parents iter-n on iter-(n-1),
    or base HEAD for iter-1), so commit.parent(0) IS the prior state — no need to
    know the branch name or resolve iter-(n-1) explicitly. iter-1 therefore diffs
    against base = "what iteration 1 changed".
  - run_cumulative_diff(repo, run_id, final_iter): base HEAD (iter-1's parent) vs
    final_iter's shadow commit = everything the run produced, final state only.
  - diff_refs(repo, from_rev, to_rev): diff two arbitrary revs (a shadow ref, sha,
    or branch). This IS the run-vs-run compare view — pass two runs' final shadow
    refs. Kept generic rather than a run_vs_run(run_a, run_b) wrapper because the
    compare view diffs whatever refs the user selects, and the caller already has
    the refs from the Iteration records.
  - DiffResult { files: Vec<FileChange>, patch: String }. FileChange { path,
    old_path (Some only for rename/copy), status: ChangeStatus, insertions,
    deletions }. Per-file summary (for the compare view's file list) + the full
    unified patch text (for the diff viewer) — the two shapes M4 consumes. Renames
    detected via diff.find_similar (renames+copies) so a move shows as one Renamed
    entry with old_path, not an add+delete pair. Per-file line counts from
    git2::Patch::from_diff(&diff, idx).line_stats(). Patch text reconstructed via
    diff.print(Patch, …), prefixing +/-/space lines with their origin char.
  - A None tree side = the empty tree (build() takes Option<&Tree>), so a
    first-iteration snapshot with no parent would show every file as added — but
    in practice iter-1 always has base HEAD as parent.
  - ChangeStatus { Added, Modified, Deleted, Renamed, Copied, Other } maps
    git2::Delta; Other folds typechange/conflicted/etc. DiffError { Git } +
    Display/Error/From<git2::Error>, mirroring the other gitx modules' shape.
- crates/gitx/src/lib.rs: pub mod diff + re-exports (diff_refs, iteration_diff,
  run_cumulative_diff, ChangeStatus, DiffError, DiffResult, FileChange).
- No new deps (git2 already the crate's only runtime dep; tempfile dev-dep present).
- Tests (5, all passing): iteration_diff iter-1 vs base (modified + added file,
  insertions counted, +changed/+fresh in patch); iteration_diff iter-2 shows only
  iter-2's changes (a.txt -one/+two, b.txt added, NOT iter-1's a.txt add);
  run_cumulative base..final (a.txt is Added across the run, final "two" only, no
  intermediate "one"); diff_refs between two separate runs' final refs (-alpha/
  +beta); detects_deletion_and_rename (rename with old_path set + a deletion in
  one diff). NB: diff tests use distinct run-ids (diff-r1..r4, run-a/run-b) to
  avoid colliding with shadow.rs's run-1..5 on the shared pid-keyed temp index
  lock when the two test modules run in parallel.

Verification:
- CARGO_HOME=/tmp/loopfleet-cargo cargo test -p loopfleet-gitx — 20 passed
  (5 diff + 6 shadow + 9 worktree/repo).
- cargo clippy -p loopfleet-gitx --all-targets — clean.
- cargo build --workspace — Finished (Tauri app included).

Design notes:
- One build() over Option<&Tree> pairs, three ref-resolving wrappers: adding a
  new diff view later means resolving refs, not re-implementing diff/patch/rename
  logic.
- Reusing commit.parent(0) as the iteration's "from" side leans on the
  snapshotter's parent chain being real; the two modules are coupled by that
  invariant, which is exactly why the snapshotter builds the chain.

Next highest-priority task: M2's last bullet — the flagged-stale ".git-grant"
regression test, now rewritten per its REVISIT note as "agent CANNOT write parent
.git while the app's out-of-sandbox actor still commits" (the snapshotter + this
diff service prove the actor commits out-of-sandbox; the sandbox renderer already
excludes the .git grant). After that, M2 is complete and M3 (the loop) begins.

2026-07-06 — M2: Sandbox .git write-boundary regression test (completes M2)
- Finished the last M2 task. The previous iteration committed the test itself
  (crates/core/tests/git_boundary.rs, commit 47d8d22) but left the bookkeeping
  undone: Cargo.lock uncommitted, PRD box unchecked, no progress entry. This
  iteration verified the work, checked the box, and committed the remainder.
- The test (rewritten per the REVISIT note; supersedes the stale ".git-grant
  succeeds" bullet) locks the boundary from both sides in one #[ignore]d,
  macos-only integration test in core (the only crate that sees both sandbox
  and gitx):
  - App-actor half (runs unconditionally): cut a worktree via worktree::add,
    write a file, shadow::snapshot() it — the shadow ref resolves via rev-parse.
    Proves commits are app-owned and the agent needs no .git write.
  - NEGATIVE (confined): wrap /usr/bin/touch <parent>/.git/hooks/pwned in the
    rendered Seatbelt profile (worktree + progress dir only; temp_dirs cleared
    deliberately — the test repo lives under /var/folders, so the default temp
    grants would sweep the parent .git into the boundary and defeat the test).
    Asserts non-zero exit AND the hook file absent — the exact hooks/-planting
    escape the design closes.
  - POSITIVE control (confined): the same profile allows touch inside the
    worktree — proves a real boundary, not a blanket deny.
  - Nested-sandbox skip: if sandbox_apply is denied (the test itself running
    under an outer Seatbelt), the confined halves skip with a message; the
    app-actor half still runs. Run outside a sandbox to observe the deny.
- crates/core/Cargo.toml: loopfleet-sandbox added as a DEV-dependency only —
  core's runtime dependency graph is unchanged; only the boundary test needs
  to render/wrap.

Verification:
- CARGO_HOME=/tmp/loopfleet-cargo cargo test --workspace — 89 passed, 6 ignored
  (42 adapters, 20 gitx, 11 sandbox, 7 core, 9 store).
- cargo test -p loopfleet-core --test git_boundary -- --ignored — 1 passed.
  In THIS env the confined checks skip (the outer agent sandbox denies nested
  sandbox_apply — same limitation as the other two ignored sandbox-exec tests);
  the app-actor snapshot half ran for real. Observing the actual deny requires
  a run outside any sandbox.
- cargo clippy --workspace --all-targets — clean.
- cargo build --workspace — Finished (Tauri app included).

M2 is COMPLETE — profile template + renderer, Sandbox trait + SeatbeltSandbox,
worktree manager, shadow-ref snapshotter, diff service, and the write-boundary
regression test are all done.
Next highest-priority task: M3 — Supervisor: run lifecycle state machine,
per-run process-group spawning, SIGTERM handling, one git actor serializing
mutations. (First M3 bullet; the iteration loop builds on it.)

2026-07-06 — M3: Supervisor foundations (state machine, process groups, git actor)
- Completes the first M3 bullet. Three pieces; the git actor was written in a
  prior uncommitted iteration and is committed here alongside the two new ones.
- crates/gitx/src/actor.rs (GitActor): the single serialized mutation point.
  One worker thread drains a bounded (64) tokio mpsc of type-erased jobs via
  blocking_recv (same spawn_blocking-style model as the store's event-log
  writer), so concurrent runs never collide on git lockfiles (index.lock, refs,
  worktree metadata). git2 reads (diff/status/log) stay concurrent and never go
  through it. Public surface is TYPED — one async method per mutating op in the
  crate (worktree_add / worktree_remove / cleanup_orphans / snapshot), private
  exec() is the only doorway to the serialization point, so a caller cannot
  sneak an arbitrary git mutation past it. Clone-able handle; worker exits when
  the last clone drops. gitx/Cargo.toml gains tokio (+ rt-multi-thread dev).
  Tests (3): full lifecycle through the actor (add→snapshot→remove→prune), 8
  concurrent submitters never overlap on the worker thread (shared busy-flag
  trips on any parallel execution), two runs snapshotting one repo concurrently
  both succeed (the lockfile-collision scenario the actor exists for).
- crates/core/src/supervisor.rs — RunState: the lifecycle state machine
  queued → running → (completed | failed | stopped). as_str/from_token match the
  runs.status text tokens the store persists; transition() validates edges so an
  illegal move (queued→completed, resurrecting a terminal state, self-loop) is a
  caught InvalidTransition, not a silently corrupt row. is_terminal() drives
  "run is over". Implemented the PRD diagram EXACTLY — no speculative queued→failed
  edge; setup failure goes queued→running→failed. from_token named to avoid the
  should_implement_trait clippy lint (Option semantics, not FromStr).
- crates/core/src/supervisor.rs — RunProcess: per-run process-group spawning +
  group-wide SIGTERM. spawn() sets process_group(0) so the child leads a fresh
  group (pgid == pid); terminate() killpg(pgid, SIGTERM) hits the WHOLE group,
  so a child the agent forked (a shell, a language server) dies too — not just
  the direct child. wait() reaps. This is the PRD's "SIGTERM that group at the
  next iteration boundary" mechanism. Unix-only (cfg via the macOS-only v1
  target; the process/sandbox layers are the platform seam). Uses tokio's
  inherent Command::process_group + libc::killpg (std can't signal a group).
  core/Cargo.toml gains tokio (process) + libc; dev-deps add tokio time/io-util/
  rt-multi-thread.
  Test: terminate_kills_the_whole_group spawns `sh -c 'sleep 300 & echo $!>f;
  wait'`, reads the backgrounded sleep's pid, terminates, then asserts the
  grandchild is gone via kill(pid,0)==ESRCH — proving the signal reaches forked
  descendants, the reason the group exists. + 3 state-machine unit tests.
- lib.rs: pub mod supervisor + re-exports (RunState, RunProcess,
  InvalidTransition); GitActor re-exported from gitx. Updated both crate docs.
- DESIGN / scope boundary: this bullet delivers the supervisor's PRIMITIVES
  (state machine + process-group control + serialized git actor). The driving
  loop that composes them with an AgentAdapter + SeatbeltSandbox + the GitActor
  to run N iterations is the NEXT M3 bullet (Iteration loop) — deliberately not
  started here to keep one task per iteration. No orchestrating Supervisor struct
  was created yet: it would be empty scaffolding until the loop gives it work;
  it materializes with the loop, holding the actor/adapter/sandbox.
- NB on adapter integration: the three adapters currently spawn their agent
  process directly (no process group, no sandbox wrapping). Threading RunProcess
  + SeatbeltSandbox::wrap through the adapter spawn is the iteration loop's job;
  the frozen AgentAdapter trait returns only a RunHandle{events}, so how stop
  reaches the adapter's child is an open integration point to resolve there.

Verification:
- CARGO_HOME=/tmp/loopfleet-cargo cargo test --workspace — 96 passed, 6 ignored
  (42 adapters, 11 core, 23 gitx, 11 sandbox, 9 store).
- cargo clippy --workspace --all-targets — clean, 0 warnings.
- cargo build --workspace — Finished (Tauri app included).

Next highest-priority task: M3 — Iteration loop: N passes, fresh agent
invocation each pass seeded with the bound task + prior progress file, app-owned
snapshot between passes, stop conditions (STATUS: COMPLETE / N reached /
failure). This composes RunState + RunProcess + GitActor with an adapter and the
sandbox, and is where the adapter-spawn/process-group/sandbox integration above
gets resolved.

2026-07-07 — M3: Iteration loop (N passes, snapshot between passes, stop conditions)
- Completes the second M3 bullet. The prior commit (22d02c9 "add AgentAdapter
  trait and run loop logic") had added crates/core/src/adapter.rs and
  crates/core/src/run_loop.rs as FILES but never wired them in: core/lib.rs had
  no `pub mod adapter/run_loop`, so they were orphan (uncompiled) source; the
  adapters crate still defined its OWN duplicate AgentAdapter trait; and
  core/Cargo.toml lacked async-trait. This iteration finished the refactor,
  verified it, checked the box, and appended this entry.
- WHY the trait moved crate: the driving loop lives in core (PRD architecture
  places "Supervisor / run loop" AND "AgentAdapter trait" in Rust core), and it
  composes over a `&dyn AgentAdapter`. adapters depends on core (for
  NormalizedEvent), so core cannot depend on adapters — the trait had to live in
  core to break the cycle. The concrete adapters (Claude/pi/cursor/stub) now
  `impl loopfleet_core::AgentAdapter`.
- crates/core/src/adapter.rs: the AgentAdapter trait + its launch/handle types
  (RunSpec, SessionSeed, RunHandle, SessionHandle, AdapterError), moved verbatim
  from adapters/lib.rs. Object-safe via async_trait so the supervisor holds a
  Box<dyn AgentAdapter> chosen by agent name.
- crates/adapters/src/lib.rs: the local trait/type definitions are DELETED and
  replaced with `pub use loopfleet_core::{AdapterError, AgentAdapter, RunHandle,
  RunSpec, SessionHandle, SessionSeed}`. The four impl files (claude/cursor/pi/
  stub) are UNCHANGED — they import via `use crate::{…}`, which now resolves to
  the re-export, and construct RunHandle { events } through its pub field. Also
  dropped lib.rs's now-orphan imports (std::path, async_trait, mpsc,
  NormalizedEvent). adapters/Cargo.toml untouched (its mpsc/async-trait use was
  already covered).
- crates/core/src/run_loop.rs (run_loop): drives an adapter through up to
  cfg.max_iterations passes against ONE bound task. Each pass: read the external
  progress file → build_prompt(task + progress-file protocol + prior progress as
  the fresh-context durable memory) → adapter.start_run → drain the normalized
  event stream (each event handed to an `on_event(pass_n, &event)` callback so
  the loop stays decoupled from the store; the caller serializes+forwards to the
  EventSender) → GitActor.snapshot the worktree to
  refs/agentapp/run-<id>/iter-<n> (app-owned; the agent never commits) → check
  the progress file for a line == "STATUS: COMPLETE".
  - Stop conditions → LoopOutcome { state, iterations }: marker present →
    RunState::Completed; max_iterations exhausted still incomplete →
    RunState::Failed; adapter cannot spawn OR a snapshot fails → RunState::Failed
    (a hard crash). A per-pass Failed *event* is deliberately NOT fatal — the
    ralph pattern retries with fresh context each pass; only exhausting N, or an
    inability to spawn/snapshot at all, fails the run.
  - LoopConfig { run_id, repo, worktree, progress_path, task_text,
    max_iterations }; IterationRecord { n, shadow_ref, commit } for the store's
    Iteration rows.
  - DEFERRED integration (documented in the module): today start_run owns the
    agent process and "stop" is dropping the RunHandle (every adapter honors that
    by killing its child). Threading SeatbeltSandbox command-wrapping +
    process-group SIGTERM THROUGH the adapter spawn — so a forked descendant dies
    too and the Seatbelt profile is the boundary — is the last M3 bullet's job
    (end-to-end wiring, where a real sandboxed agent is spawned). This module is
    transport- and sandbox-agnostic on purpose, and is proven end-to-end against
    a scripted in-test adapter driving a REAL git worktree + GitActor snapshots.
- core/Cargo.toml: added async-trait (for the moved trait) and tokio "sync"
  feature (mpsc in the handles).
- Tests (4 in run_loop, all passing) — each cuts a real repo+worktree via the
  GitActor and runs a scripted adapter that writes the progress file + touches
  the worktree per pass: completes_when_marker_written (marker on pass 1 →
  Completed, 1 iteration, shadow ref named, all 4 events forwarded tagged pass 1);
  fails_after_exhausting_iterations (never completes, max 3 → Failed, 3 chained
  distinct shadow commits); hard_failure_when_adapter_cannot_spawn (start_run Err
  → Failed, 0 iterations); prompt_carries_task_and_prior_progress (pass 1 prompt
  has the task + "(no prior progress yet)", pass 2 prompt feeds back pass 1's
  progress line).

Verification:
- CARGO_HOME=/tmp/loopfleet-cargo cargo test --workspace — 100 passed, 6 ignored
  (42 adapters, 15 core [+4 run_loop], 23 gitx, 11 sandbox, 9 store).
- cargo clippy --workspace --all-targets — clean, 0 warnings.
- cargo build --workspace — Finished (Tauri app included).

Design notes:
- on_event is a plain &mut dyn FnMut(u32, &NormalizedEvent), not an EventSender:
  the loop stays store-agnostic and unit-testable with a Vec collector, while the
  real caller closes over an EventSender and does serialize+send. Keeps the
  core→store dependency from leaking the event-log channel into the loop's core.
- The completion check reads the FILE, not the event stream: "done = the marker
  appears" (PRD open-question 2) is uniform across all agents, so the loop needs
  no per-adapter done-predicate — the marker in the external progress file is the
  single source of truth.

Next highest-priority task: M3 — Plan parser (PRD.md checkboxes, frozen, authored
state only + alternative plans-folder convention; text-based task anchors). Then
the progress-file watcher, then the end-to-end "Run N loops on task X" against a
fixture repo with the Claude Code adapter (where the sandbox + process-group
integration deferred above gets resolved).

2026-07-07 — M3: Plan parser (PRD.md checkboxes + plans-folder; text anchors)
- Completes the third M3 bullet. Pure, deterministic parsing (no inference,
  no side effects) landing in core as crates/core/src/plan.rs — the plan is
  domain input the supervisor/run loop consume, so it belongs beside them.
- parse_plan(&str) -> ParsedPlan { title, tasks[] }. title = first level-1
  heading (`# …`); ## and lower ignored, first H1 wins. tasks = markdown
  checkboxes in document order. Each ParsedTask { anchor: TaskAnchor
  { normalized_text, line_hint }, text, checked } mirrors the store's `tasks`
  table shape exactly (plan_id + normalized_text PK, line_hint tiebreaker,
  text display, checked authored-input), so persistence later is a direct map.
  - Checkbox recognition (parse_checkbox): optional indentation, a `-`/`*`/`+`
    list marker + space, a `[ ]` / `[x]` / `[X]` box, then NON-EMPTY text.
    `[]` (no inner char), empty-text boxes, and plain bullets are rejected.
    Returns the outer-trimmed display text.
  - Anchor identity = normalize(text): trim, collapse internal whitespace to
    single spaces, lowercase. Resilient to cosmetic whitespace/case edits so a
    run's task binding survives them. line_hint is the 1-based file line — a
    hint/tiebreaker for locating the task, never the key (PRD: Plans).
  - Fenced code blocks (``` and ~~~) are tracked and their contents skipped, so
    a checkbox shown inside a code EXAMPLE is never mistaken for a real task.
    (The PRD/data-model fenced blocks contain no checkbox-shaped lines, but this
    closes a real false-positive class deterministically.)
- discover_plans(repo, PlanConvention) -> Vec<PathBuf>: the two conventions.
  Prd → <repo>/PRD.md if present (0/1). Folder → every *.md under <repo>/plans/,
  sorted by path (stable order); non-.md ignored; a missing folder is an empty
  list, not an error. PlanConvention::from_token maps the persisted
  plan_convention string ("prd"|"folder"). parse_plan_file(path) = read + parse.
- SCOPE (Simplicity First): the parser extracts title + tasks. "Free-form
  sections" from the PRD's parser description are intentionally NOT modelled —
  nothing consumes them (M4's plan view renders the raw frozen markdown) and the
  data model's Plan stores only parsed_tasks[]. Persisting parsed plans into the
  store (a plans.rs) is deferred to when M4 wiring needs it; this task is the
  parser + file discovery, matched to the existing tasks-table shape.
- Types derive serde (Serialize/Deserialize) so they cross the Tauri boundary to
  the M4 plan view unchanged. lib.rs: pub mod plan + re-exports (discover_plans,
  parse_plan, parse_plan_file, ParsedPlan, ParsedTask, PlanConvention,
  TaskAnchor). No new deps (serde already present; tempfile dev-dep already there
  for the discovery tests).
- Tests (12, all passing): mixed checkbox states (- / * markers, x/X); first-H1
  title ignoring H2 + no-title case; normalization collapses whitespace+case and
  records the 1-based line_hint (display text keeps its casing); code-fence
  contents skipped; non-checkbox/empty-box/`[]` rejected; indented checkbox
  parsed; discover prd finds root PRD.md (empty when absent); discover folder
  finds sorted *.md only; missing plans/ dir → empty; from_token mapping; and a
  real round-trip against THIS repo's PRD.md (title contains "Agent Cockpit",
  >20 tasks parsed, the M0 scaffold task is authored-checked).

Verification:
- CARGO_HOME=/tmp/loopfleet-cargo cargo test --workspace — 112 passed, 6 ignored
  (42 adapters, 27 core [+12 plan], 23 gitx, 11 sandbox, 9 store).
- cargo clippy -p loopfleet-core --all-targets — clean, 0 warnings.
- cargo build --workspace — Finished (Tauri app included).

Design notes:
- Normalization is deliberately moderate (whitespace-collapse + lowercase, no
  markdown stripping): aggressive stripping risks two distinct tasks colliding on
  the (plan_id, normalized_text) PK, while under-normalizing makes bindings
  brittle. Whitespace+case are the common cosmetic edits; formatting is left
  intact to keep distinct tasks distinct.
- The parser returns tasks in document order and does not dedup: anchor
  uniqueness is enforced by the store PK at insert time, keeping the parser a
  pure function of its input.

Next highest-priority task: M3 — Progress-file watcher: detect `STATUS: COMPLETE`
in the per-run external progress file → mark run completed. (The run loop already
reads the marker at each pass boundary; the watcher is the live/event-driven
detection the app needs for the run UI.) Then the end-to-end "Run N loops on task
X" against a fixture repo with the Claude Code adapter (where the deferred
sandbox + process-group integration through the adapter spawn gets resolved).

2026-07-07 — M3: Progress-file watcher (detect STATUS: COMPLETE → mark completed)
- Completes the fourth M3 bullet. The run_loop already read the marker at each
  pass boundary; this adds the live, event-driven detection the run UI needs
  plus a single home for the marker protocol so the loop and the watcher can
  never disagree on what "complete" means.
- crates/core/src/progress.rs (new module):
  - COMPLETION_MARKER = "STATUS: COMPLETE" — the one place the marker's spelling
    lives. Previously the literal was duplicated (run_loop's completion check +
    its prompt text); both now reference this constant.
  - contents_mark_complete(&str): the marker on its own (trimmed) line. Prose
    that merely mentions the marker mid-line does NOT trip it (exact trimmed-line
    match), matching the run_loop semantics it replaces.
  - file_marks_complete(&Path): read + test; a missing/unreadable file = "not
    complete" (the agent has not written it yet). The app is read-only on the
    file (PRD: Plans).
  - watch_for_completion(path, interval, cancel: oneshot::Receiver<()>) -> bool:
    the live watcher. Checks once up front (marker may already be present), then
    polls every `interval` via tokio::select! against the cancel receiver.
    Resolves true when the marker appears, false if cancel fires first (run
    ended for another reason — failed/stopped/exhausted). Caller cancels by
    sending on OR dropping the paired Sender (both wake the receiver).
- DECISION — poll, not fs-events: the progress file lives OUTSIDE the repo
  (app-managed, keyed by run-id), the marker is append-only so nothing is missed
  between polls, and polling needs no watcher crate (Simplicity First). This is
  distinct from the app-lane FileChanged worktree watcher (M4 UI concern), which
  observes the repo worktree, not this external file.
- run_loop.rs refactor: dropped the private task_marked_complete() (its logic is
  now crate::progress::file_marks_complete) and the hardcoded `STATUS: COMPLETE`
  in build_prompt (now {marker} = COMPLETION_MARKER). Behavior unchanged; the
  four run_loop tests still pass. The loop keeps its pass-boundary check (stop =
  SIGTERM at the next iteration boundary per the PRD); the watcher is the
  separate live-detection primitive the app/run UI consumes concurrently.
- lib.rs: pub mod progress + re-exports (COMPLETION_MARKER, contents_mark_complete,
  file_marks_complete, watch_for_completion).
- core/Cargo.toml: added the tokio "time" feature to the runtime deps (was only
  in dev-deps) for tokio::time::sleep in the poller.
- Tests (6 in progress, all passing): marker on its own line (incl. surrounding
  whitespace) is complete; prose mentioning it + empty are not; missing file not
  complete; present file with marker complete; watch returns true when the marker
  is already present; watch detects the marker appended after start; cancel via
  send stops watching (false); dropping the sender also cancels (false).

Verification:
- CARGO_HOME=/tmp/loopfleet-cargo cargo test -p loopfleet-core — 35 passed
  (6 progress + 4 run_loop unchanged + 25 pre-existing), 1 ignored (git_boundary).
- cargo clippy -p loopfleet-core --all-targets — clean, 0 warnings.
- cargo build --workspace — Finished (Tauri app included).

Design notes:
- watch_for_completion takes the cancel Receiver by value and selects on
  &mut cancel: oneshot::Receiver is Unpin, so no pin is needed, and a resolved
  select arm returns immediately (no re-poll of a completed future).
- The marker protocol living in its own module (not run_loop) is what lets the
  live watcher and the pass-boundary check share exactly one definition of
  "complete" — the correctness reason to extract it now rather than duplicate.

Next highest-priority task: M3's last bullet — "Run N loops on task X"
end-to-end against a fixture repo with the Claude Code adapter, where the
deferred SeatbeltSandbox command-wrapping + process-group SIGTERM integration
through the adapter spawn finally gets resolved. That completes M3.

2026-07-07 — M3: "Run N loops on task X" end-to-end with the Claude Code adapter
- Completes the last M3 bullet, resolving the sandbox-wrapping integration the
  run loop deferred. The pieces (worktree/snapshot GitActor, SeatbeltSandbox
  renderer, ClaudeAdapter, run_loop) now compose into one confined N-pass run.
- adapter.rs: finished the WIP RunSpec.wrapper field — an OPAQUE argv prefix the
  adapter prepends (`wrapper[0] wrapper[1..] program args…`). Empty = spawn the
  agent directly (dev/test); filled = the Seatbelt invocation. The adapter never
  learns the backend is Seatbelt (PRD: no Sandbox details leak into adapters).
- adapters/lib.rs: base_command(wrapper, program) -> tokio Command — the one
  place the prefix is applied. All three real adapters (claude/pi/cursor) now
  build their command via it instead of Command::new(<prog>); their now-orphan
  `use tokio::process::Command` imports were removed. stub + the three live-test
  RunSpecs gained `wrapper: Vec::new()`.
- sandbox/lib.rs: confine_prefix(&RenderParams, &Path) -> Vec<OsString> — the
  adapter-facing counterpart to Sandbox::wrap. Renders + persists the profile
  and returns [sandbox-exec, -f, <profile>], keeping the Seatbelt argv shape
  inside the sandbox crate. Extracted the shared render+write into a private
  persist_profile() that both confine_prefix and wrap() now call (was inline in
  wrap). Two unit tests: prefix shape + profile written (parent dir created);
  render errors propagate.
- core/run_loop.rs: LoopConfig gains `wrapper: Vec<OsString>`, threaded into
  every pass's RunSpec. New unit test threads_wrapper_into_each_pass_spec asserts
  the ScriptedAdapter sees the wrapper verbatim on each pass (the adapter now
  records spec.wrapper alongside the prompt).
- crates/adapters/tests/run_n_loops.rs (new, #[ignore], cfg macos): the
  end-to-end capstone. Cuts a fixture worktree via the GitActor, renders the
  Seatbelt boundary (worktree + progress dir + claude's $HOME config dirs +
  temp), builds the wrapper via confine_prefix, and drives run_loop(&ClaudeAdapter
  …) on a real task ("create DONE.txt containing 'done'"). Asserts the run reaches
  Completed within N passes, every pass's shadow ref resolves in the repo, and
  DONE.txt landed in the confined worktree. adapters gains gitx + sandbox as
  DEV-deps only (no runtime cycle). Runnable manually outside a nested sandbox
  (a nested sandbox_apply is OS-denied, same limit as the other live sandbox
  tests).
- SCOPE: process-group SIGTERM through the adapter spawn (RunProcess in
  supervisor.rs) is deliberately NOT wired here — it is the *stop* path, which
  "run N loops to completion" does not exercise; it belongs with M4's live-run
  stop button. The wrapper/sandbox integration is what makes the loop run
  confined end-to-end, which is this bullet.

Verification:
- CARGO_HOME=/tmp/loopfleet-cargo cargo test --workspace — all pass
  (42 adapters +3 ignored, 36 core [+1 run_loop wrapper], 23 gitx, 13 sandbox
  [+2 confine_prefix] +2 ignored, 9 store; +1 new ignored e2e integration test).
- cargo clippy --workspace --all-targets — clean, 0 warnings.
- cargo build --workspace — Finished (Tauri app included).
- The e2e test itself is #[ignore]d (real claude + network + outside-sandbox);
  compiled but not executed in this env.

M3 is COMPLETE — supervisor foundations, iteration loop, plan parser,
progress-file watcher, and the end-to-end "Run N loops on task X" with the
Claude adapter are all done. Next milestone: M4 — UI: overview and timeline
(project list + plan view, run timeline, live run view, compare view).

2026-07-07 — M4: Project list + plan view (derived TaskStatus overlay, launch)
- Completes the first M4 bullet. A prior interrupted iteration had left the
  backend for this task uncommitted (new overview/task_status/plans/runs modules
  + the launch_run/plan_overview Tauri commands) but it did not compile and had
  no frontend. This iteration finished it: fixed the compile, wired the plan-view
  UI, verified, checked the box, appended this entry.
- crates/core/src/task_status.rs (TaskStatus + derive_status): the four DERIVED
  per-task states (not-started / in-progress / completed-unaccepted / accepted),
  computed from the runs bound to a task — never stored (PRD: Plans / Data model).
  Precedence: any accepted → Accepted; any completed → CompletedUnaccepted; any
  queued|running → InProgress; else NotStarted. `checked` (authored input) plays
  no part. Serializes kebab-case for the UI. 6 unit tests.
- crates/core/src/overview.rs (plan_overview): the read side of the plan UI.
  Discovers the project's plan file(s) by convention, parses each
  (deterministic), upserts plan + tasks into the store so runs can bind to a
  stable (plan_id, task_anchor), then joins parsed tasks against their runs to
  derive each task's status. Returns PlanView { plan_id, file_path, title,
  markdown (raw, for the frozen render), tasks[] }; TaskView carries anchor,
  line_hint, text, checked, status, run_count. Never edits the frozen plan file.
  3 tests (overlay + task sync, missing PRD → no plans, unknown convention).
- crates/store/src/plans.rs: plan_id (deterministic from project id + file path),
  upsert_plan, upsert_task (upsert by the (plan_id, normalized_text) anchor PK;
  never deletes a vanished task — a launched run's FK may still reference it).
  3 tests. crates/store/src/runs.rs: NewRun + insert_run, update_run_status,
  insert_iteration, RunSummary + list_runs_for_plan (every run bound to any task
  in a plan, for status derivation). 3 tests.
- src-tauri/src/lib.rs: AppState now holds Arc<Mutex<Connection>> (so a
  background run task shares the single writer) + a GitActor + data_dir. Two new
  commands: plan_overview(project_id) → Vec<PlanView>; launch_run(project_id,
  task_anchor, agent, max_iterations) → run id. launch_run resolves the bound
  task, mints app-managed paths keyed by run id (worktree via the GitActor,
  external progress dir, Seatbelt profile), renders the sandbox boundary into the
  opaque wrapper prefix via confine_prefix (agent $HOME config dirs granted),
  inserts the run row, then drives run_loop on a background task — persisting
  events (via the new store::insert_event), iterations, and final status on the
  shared connection. The mutex is never held across an await.
- FIX (why it didn't compile): the launch task's event callback used
  rusqlite::params! but src-tauri doesn't depend on rusqlite. Added
  store::insert_event(conn, owner_id, json) — a synchronous single-row append on
  a borrowed connection, the app's writer path that doesn't warrant the async
  EventLog channel — keeping rusqlite out of the app layer (consistent with how
  insert_iteration/update_run_status are already called). Removed the now-orphan
  now_millis + SystemTime import from src-tauri.
- FIX (Send): run_loop is spawned on Tauri's multi-thread runtime, so its future
  must be Send; its on_event was `&mut dyn FnMut(...)` (not Send). Tightened to
  `&mut (dyn FnMut(...) + Send)`. The app's DB-writing closure (captures
  Arc<Mutex<Connection>> + String) is Send; the run_loop unit-test closures
  (Vec/borrow collectors) are Send too, so all four still compile unchanged.
- dist/index.html: the plan view. Projects are now clickable; selecting one loads
  its plan overview. Each task renders with a color-coded status badge, its run
  count, and (unless authored-checked, which gates launch) an agent selector +
  iteration count + Run button that calls launch_run. Completed-unaccepted is
  surfaced LOUDLY — an amber banner counting the review queue plus amber row
  highlighting (PRD: "surface completed-unaccepted loudly"). The frozen plan
  source is shown verbatim in a collapsible <pre> (no markdown dependency pulled
  — Simplicity First). Dependency-free vanilla JS, matching the existing shell.

Verification:
- CARGO_HOME=/tmp/loopfleet-cargo cargo test --workspace — 138 passed, 7 ignored
  (42 adapters, 45 core [+6 task_status +3 overview], 23 gitx, 13 sandbox, 15
  store [+3 plans +3 runs]).
- cargo clippy --workspace --all-targets — clean, 0 warnings.
- cargo build --workspace — Finished (Tauri app included).
- The UI itself is a Tauri desktop GUI (can't be driven headlessly in this env);
  the commands it calls are exercised by the core/store tests, and the frontend
  is dependency-free vanilla JS served statically.

Design notes:
- TaskStatus lives in core (derived domain state), computed at read time in
  overview.rs from run records — the store never persists it, keeping the PRD's
  "derived, never stored as truth" invariant honest.
- The launch flow deliberately releases the DB mutex before every await
  (git worktree add, the run loop) and re-locks only for synchronous inserts, so
  a long-running background run never blocks the UI commands on the single writer.

Next highest-priority task: M4 — Run timeline: iterations as rows, per-iteration
events, per-iteration diff viewer. (The event log + iteration/shadow-ref records
this task now writes are exactly what the timeline reads back.)

2026-07-07 — M4: Run timeline (iterations as rows, per-iteration events + diff)
- Completes the second M4 bullet. The read side that turns the event log +
  iteration/shadow-ref records (written by launch_run) into a per-run timeline:
  iterations as rows, each with the normalized events that occurred during it
  and the diff it produced. App is read-only throughout (no mutation).
- KEY: per-iteration event attribution via the (already-schema'd but unused)
  iterations.event_log_offset column. Events are one flat, seq-ordered log per
  run; each iteration now records the seq of its LAST event, so the timeline
  partitions the log back into passes (iteration n owns events with
  offset(n-1) < seq <= offset(n)). This is exactly what the PRD data model's
  Iteration.event_log_offset was for.
- store/runs.rs:
  - insert_iteration gains an event_log_offset: Option<i64> param (persisted to
    the column). Sole callers: src-tauri launch_run + store's own tests.
  - IterationRow { n, shadow_ref, event_log_offset } + load_iterations(conn,
    run_id) — a run's iterations in pass order.
  - RunDetail { id, task_anchor, agent, status, max_iterations, repo_path } +
    load_run(conn, run_id) -> Option<RunDetail>. Joins runs → plans → projects
    to recover the PARENT repo path (where the shadow refs live), which the
    timeline diffs against. QueryReturnedNoRows → Ok(None).
  - RunSummary gains #[derive(Serialize)] so plan_runs can return it over IPC.
- gitx/diff.rs: iteration_diff_at(repo_path, run_id, iter) — a path-taking
  wrapper over iteration_diff that opens the repo itself, so core (which holds a
  path, not a git2::Repository) needs no direct git2 dependency.
- core/timeline.rs (new module): run_timeline(conn, run_id) -> RunTimeline.
  Loads the run detail, its iterations, and its full event log; partitions the
  events into iterations by offset (the LAST iteration sweeps any trailing
  events so nothing is dropped from the view); computes each iteration's diff
  via iteration_diff_at (a missing/unreadable shadow ref → diff: None, not a
  hard error). Serializable view structs (RunTimeline, IterationView,
  TimelineEvent { seq, ts, event: serde_json::Value }, DiffView, FileChangeView)
  — TimelineEvent passes the stored {"kind":…} payload through as a Value
  (robust to schema drift; the frontend already speaks that shape). TimelineError
  { NotFound, Store }. Mirrors overview.rs (read-side assembly lives in core).
  Added serde_json to core's runtime deps.
- src-tauri/lib.rs:
  - launch_run now captures each pass's event boundary: the on_event closure,
    under the SAME lock as the insert, reads conn.last_insert_rowid() after each
    insert_event (events.seq == rowid) into a per-pass map; the post-run
    iteration inserts pass offsets.get(&it.n). Capturing inside the held lock is
    what makes last_insert_rowid correct even though other UI commands share the
    connection (upsert_task etc. would otherwise clobber it).
  - Two new commands: plan_runs(planId) -> Vec<RunSummary> (runs bound to a
    plan, grouped by task in the UI) and run_timeline(runId) -> RunTimeline.
    Registered in the invoke_handler.
- dist/index.html: each task now lists its runs (status dot + short id +
  Timeline button); Timeline loads run_timeline and renders it below the plan —
  iterations as collapsible rows (summary: event count · file count · +ins/−del),
  each expanding to the per-iteration event stream (kind + a by-kind detail) and
  the diff (per-file status + line counts, plus a collapsible unified patch).
  Dependency-free vanilla JS, matching the existing shell; new CSS for the run
  list + timeline. Iteration 1 opens by default; Close clears the panel.
- Tests (3 new core timeline + 1 new store load_run, all passing): events group
  by offset (2+3 across two iterations, {"kind"} payload preserved); trailing
  events with a short offset still land on the last iteration (nothing dropped);
  unknown run → NotFound; load_run joins repo_path and returns None for a ghost.
  The offset-partition tests point repo_path at a non-git temp dir so diffs
  resolve to None — exercising the core assembly without a live git fixture
  (iteration_diff itself is already covered by gitx's diff tests).

Verification:
- CARGO_HOME=/tmp/loopfleet-cargo cargo test --workspace — all pass (42 adapters
  +3 ignored, 48 core [+3 timeline], 23 gitx, 13 sandbox +2 ignored, 16 store
  [+1 load_run]).
- cargo clippy --workspace --all-targets — clean, 0 warnings.
- cargo build --workspace — Finished (Tauri app links).
- Frontend JS parses (node --check of the <script> block); the Tauri GUI can't
  be driven headlessly here, but its two new commands are exercised by the
  core/store tests and the JS is dependency-free.

Design notes:
- event_log_offset is the LAST event's seq (an inclusive upper bound), captured
  at snapshot time = after the pass's events are all inserted, so the partition
  (offset(n-1), offset(n)] is exact. Recording it under the insert's lock is the
  correctness crux for last_insert_rowid on the shared single-writer connection.
- Diffs degrade to None rather than failing the whole timeline: a run may have
  iteration rows whose shadow ref was pruned or is otherwise unreadable, and the
  event stream is still worth showing.

Next highest-priority task: M4 — Live run view: streaming events, current file
changes, stop button. (This timeline reads persisted state; the live view needs
event streaming to the UI + the process-group SIGTERM stop path that M3
deliberately deferred to this bullet.)

2026-07-07 — M4: Live run view (streaming events, file changes, stop button)
- Completes the third M4 bullet. Turns launch_run from a fire-and-persist
  background loop into a live-observable one, and wires the stop path M3
  deliberately deferred ("process-group SIGTERM ... is the job of M4's live-run
  stop button"). Three pieces: cancellation in the loop, a group-SIGTERM stop in
  the adapters, and a git-status worktree watcher for the app-lane FileChanged.
- core/run_loop.rs — cancellation:
  - run_loop gains a `cancel: &mut watch::Receiver<bool>` param. Boundary stop
    (PRD default, "SIGTERM that group at the next iteration boundary"): before
    spawning each pass, if cancel is set → return RunState::Stopped (nothing is
    running at the boundary, so it is clean). Mid-pass: the drain is now a
    tokio::select! over handle.events.recv() vs cancel.changed(); a stop breaks
    the drain, then drop(handle) closes the receiver (the adapter SIGTERMs the
    agent) and the pass is STILL snapshotted before returning Stopped — PRD
    "keep all shadow refs". changed()==Err (all senders dropped, i.e. app
    shutdown) is also treated as stop.
  - Updated the module NB (was "process-group SIGTERM is deferred to M4's stop
    button") to describe the now-wired stop; the five existing tests gained a
    `watch::channel(false)` arg; two new tests: stops_at_boundary_when_cancelled
    (stop before the loop → Stopped, 0 iterations, 0 spawns) and
    stop_requested_mid_run_ends_after_the_current_pass (stop during pass 1 →
    Stopped with pass 1's shadow ref kept, not exhausting all 5).
- adapters — process-group stop:
  - base_command now sets process_group(0) (cfg(unix)) so every agent (or its
    sandbox-exec wrapper, which pgid==pid) leads its own group. New pub(crate)
    stop_agent(child): killpg(pid, SIGTERM) on unix — replaces the three
    identical `child.start_kill()` calls in the claude/pi/cursor drive()
    receiver-drop branches. So dropping the RunHandle now SIGTERMs the whole
    group (shells/tools the agent forked die too), matching the PRD stop
    semantics. libc added as a cfg(unix) dep. Robust reaping of an agent that
    ignores SIGTERM stays M6 (orphan reaping).
  - KNOWN LIMIT (documented, acceptable for v1): the drop-kill only fires when
    the drive task next attempts a send, so a stop during a fully quiet agent
    isn't delivered until the agent emits again or the pass ends (then the
    boundary stop catches it). The existing mechanism; mid-pass responsiveness
    is best-effort.
- gitx/status.rs (new) — worktree_changes(worktree) -> Vec<String>: paths that
  differ from HEAD (staged/unstaged/untracked), via git2 statuses (a read; PRD
  "git2 for reads"). This is the observation source for the app-sourced
  FileChanged lane — never parsed from the agent stream, so it catches files
  changed by shell commands too (PRD). 1 test (edits + untracked reported, clean
  tree empty). Exported from gitx/lib.rs.
- src-tauri/lib.rs — the live wiring:
  - AppState gains `stops: Arc<Mutex<HashMap<String, watch::Sender<bool>>>>` — a
    cancel sender per active run. New stop_run(run_id) command sends true on it;
    unknown/ended run → "run is not active".
  - record_event(db, app, run_id, ev) -> Option<i64>: the one place an event is
    persisted (insert_event, seq captured under the same lock) AND emitted to the
    UI (`run_event` Tauri event, RunEventPayload{run_id, seq, event: Value}).
    Both the loop's on_event closure and the file watcher funnel through it.
  - launch_run: takes AppHandle; registers a watch::channel in `stops`; spawns a
    1 Hz worktree-status poller (dedup via a HashSet, emits FileChanged for each
    newly-changed path) alongside run_loop; passes cancel_rx to run_loop; on
    completion aborts the poller, removes the cancel sender, persists
    iterations/status as before, and emits `run_status` (RunStatusPayload) so the
    live view can finalize. tokio (sync,time) added as an app dep.
  - capabilities/default.json: added core:event:default so the frontend may
    listen() for the two emitted events.
- dist/index.html — the live view: a Run launch now opens a live panel (reusing
  the .timeline shell) with a status dot, a Stop button (invoke stop_run), a
  Close button, a files-changed list, and a scrolling event stream. Two global
  listen()s ("run_event"/"run_status") dispatch to the current live run by
  run_id: FileChanged paths append to the files list (deduped), other events
  render via the existing eventDetail(); a terminal status disables Stop and
  reloads the plan so the task's run dot updates. Dependency-free vanilla JS.

Verification:
- CARGO_HOME=/tmp/loopfleet-cargo cargo test --workspace — 145 passed, 7 ignored
  (adapters 42 +3, core 50 [+2 stop], gitx 24 [+1 status], sandbox 13 +2, store 16).
- cargo clippy --workspace --all-targets — clean, 0 warnings.
- cargo build --workspace — Finished (Tauri app links).
- Frontend JS parses (new Function over the <script> block); the Tauri GUI can't
  be driven headlessly here, but its new command (stop_run) + emit path are
  exercised by the core/gitx tests and the JS is dependency-free.

Design notes:
- Boundary stop needs no process kill (nothing runs between passes); the group
  SIGTERM only matters for the mid-pass drop path. Both keep the pass's snapshot,
  so shadow refs survive a stop either way.
- record_event unifies persist+emit so the live stream and the persisted log can
  never diverge, and the per-pass offset (last_insert_rowid) stays correct under
  the shared single-writer connection even with the file watcher interleaving
  FileChanged inserts (its seq just lands in whichever pass window it falls).

Next highest-priority task: M4's last bullet — Compare view: two-or-more runs
side by side, final-ref diffs, "use this run" → merge the chosen branch into a
user-named target. That completes M4.

2026-07-08 — M4: Compare view (side-by-side final-ref diffs + "use this run")
- Completes the last M4 bullet, so M4 is done. Two halves: a read side (the runs
  bound to a task, side by side, each with its produced diff) and a mutation side
  ("use this run" → merge the chosen run's final state into a user-named target
  branch + mark it accepted). App stays read-only on plans/progress; the only
  writes are the app-owned merge (via the git actor) and the acceptance flag.
- KEY design point — what "the chosen run's branch" actually is: the agent never
  commits, so the `agent/<run-id>` branch still points at base HEAD; the run's
  work lives in its final shadow commit (refs/agentapp/run-<id>/iter-<n>). So the
  merge source is the final shadow ref, not the branch. "Use this run" = take
  that final commit and put it on the named target.
- crates/gitx/src/merge.rs (new): merge_run(repo, source_rev, target_branch,
  scratch_root) -> MergeResult { target_branch, merged_commit, created,
  up_to_date }. A mutation, so it shells out (consistent with worktree/shadow)
  and funnels through the GitActor. Two cases:
  - target branch absent → `git branch <target> <final_commit>` (pure ref
    creation; no worktree, no possible conflict — the common "use this run"
    flow). created=true.
  - target branch present → a real `git merge --no-edit <final_commit>` performed
    inside a THROWAWAY worktree under scratch_root (keyed by pid+nanos), so the
    user's own checkout is never touched. A conflicting merge is `merge --abort`ed
    and the worktree torn down, leaving the target exactly as it was →
    MergeError::Conflict (conflict assistance is post-v1). Checking the target
    out also naturally guards against merging into a branch that is checked out
    in the main worktree (git refuses the second checkout).
  MergeError { Io, Git(stderr), Conflict(msg) } + Display/Error/From<io::Error>,
  mirroring the other gitx modules. Re-exported from gitx/lib.rs; GitActor gains a
  typed merge_run(...) method (the only doorway to the serialized mutation point).
  4 tests: creates target at the run's final commit (file lands on the new
  branch); merges into an existing integration branch (run file + base file both
  present, throwaway worktree gone); conflicting merge aborts and leaves the
  target unchanged with no lingering worktree; missing source ref → Git error.
- crates/gitx/src/diff.rs: run_cumulative_diff_at(repo_path, run_id, final_iter)
  — a path-taking wrapper over run_cumulative_diff (mirrors iteration_diff_at) so
  core (which holds a path, not a git2::Repository) computes each run's produced
  diff without a direct git2 dep. Re-exported.
- crates/store/src/runs.rs: set_run_accepted(conn, run_id) — UPDATE runs SET
  accepted = 1 (idempotent). Acceptance is a separate flag from status (PRD data
  model): "Implemented" = a run you accepted. Re-exported. 1 test.
- crates/core/src/compare.rs (new): compare_view(conn, plan_id, task_anchor) ->
  CompareView { task_anchor, runs: Vec<RunCompare> }. RunCompare { run_id, agent,
  status, accepted, final_ref, diff }. For each run bound to the task: recover its
  parent repo (load_run) + last iteration (load_iterations), compute the
  base..final cumulative diff via run_cumulative_diff_at (a missing/unreadable
  shadow ref → diff:None, not a hard error), and expose the final shadow ref.
  This is the "two runs both complete on the same task" resolution (PRD open Q3):
  show both diffs, never score. Reuses timeline::to_diff_view (made pub(crate)) so
  the DiffView/FileChangeView shape the UI already speaks is shared, not
  duplicated. CompareError { Store } + Display/Error/From. 2 tests (only the
  task's runs appear, final_ref is the last iteration's shadow ref, a run with no
  iterations has no final ref; empty when no runs).
- src-tauri/src/lib.rs: two commands.
  - compare_task(planId, taskAnchor) -> CompareView (read).
  - use_run(runId, targetBranch) -> UseRunResult { target_branch, merged_commit,
    created, up_to_date }: trims/validates the target (non-empty), resolves the
    run's repo + final shadow ref (the last iteration with a ref; "run has no
    snapshot to use" otherwise), calls git.merge_run through the actor with
    scratch_root = data_dir/worktrees, then set_run_accepted. The DB mutex is
    released before the await (same discipline as launch_run). UseRunResult is a
    local serde payload (MergeResult isn't Serialize and gitx has no serde dep).
    Both registered in the invoke_handler.
- dist/index.html: each task with ≥1 run gets a "Compare runs"/"Review run"
  button → showCompare, rendering the runs as side-by-side columns in a new
  #compare panel (reusing the .timeline shell). Each column: status/accepted dot,
  short id · agent, the final-ref diff summary (files · +ins/−del) + file list +
  collapsible patch, and a "Use this run" row (target-branch input + button,
  disabled when there's no snapshot). Success sets a status message (created vs
  merged vs up-to-date), reloads the plan (so the task's dot flips to accepted),
  and re-renders the compare view. Target branch is never prefilled (PRD: never
  targets your main branch by default). currentPlans hoisted to module scope so
  the post-merge reload can re-resolve the plan. New CSS for .cmpcols/.cmpcol/
  .userun. Dependency-free vanilla JS, matching the existing shell.

Verification:
- CARGO_HOME=/tmp/loopfleet-cargo cargo test --workspace — all pass (adapters 42
  +3 ignored, core 52 [+2 compare], gitx 28 [+4 merge], sandbox 13 +2 ignored,
  store 17 [+1 accept]).
- cargo clippy --workspace --all-targets — clean, 0 warnings.
- cargo build --workspace — Finished (Tauri app links).
- Frontend JS parses (new Function over the <script> block); the Tauri GUI can't
  be driven headlessly here, but its two new commands are exercised by the
  core/gitx/store tests and the JS is dependency-free.

Design notes:
- The new-branch case needs no worktree and cannot conflict, which is why it's a
  distinct path from the existing-branch merge; the throwaway worktree only pays
  its cost when a real merge is required, and it keeps the user's checkout and
  the app's "commits are app-owned, no user branch is auto-advanced" invariant
  intact — the app touches only the branch the user explicitly named.
- Acceptance is set only after a successful merge, so a conflicting or failed
  "use this run" never marks the run accepted; the derived TaskStatus flips to
  Accepted purely off the persisted flag (task_status already reads it).

M4 is COMPLETE — project/plan view, run timeline, live run view, and the compare
view + "use this run" are all done. Per the PRD build order the next work is the
M6 hardening bits (crash recovery, agent discovery/version checks, settings)
before the tool goes into daily use; M5 (plan chat) stays deferred.

2026-07-08 — M6: Crash recovery + orphan cleanup on startup (mark running runs failed)
- First M6 hardening bullet. Per the PRD build order (M4 → M6 hardening bits →
  daily use → M5), this is the highest-priority remaining task now that M0–M4 are
  done. Scope = the bullet's explicit parenthetical: mark running runs failed on
  restart, keep refs — plus wiring the orphan-worktree prune the M2 worktree
  manager built but never called at startup.
- crates/store/src/runs.rs: fail_interrupted_runs(conn) -> Vec<String>. One
  `UPDATE runs SET status='failed' WHERE status IN ('queued','running')
  RETURNING id`. Runs don't survive an app restart in v1 (the daemon split is
  post-v1), so any run still marked in-flight was interrupted by a prior crash or
  quit — its background task and agent process are gone. Only runs.status is
  touched: iterations and the app-owned shadow refs are left INTACT (PRD "keep
  refs"). Returns the affected ids (for the startup log + testability). Idempotent
  — a second startup finds nothing to recover. Re-exported from store/lib.rs.
- src-tauri/src/lib.rs setup(): after opening the DB and BEFORE managing state,
  call fail_interrupted_runs (sync, on the fresh connection) and log the count.
  Then read the registered projects' repo paths and spawn a background task that
  runs git.cleanup_orphans (git worktree prune) per project — the "orphan cleanup
  on startup" wiring. GitActor is now spawned in setup() (was inline in the
  manage() literal) so its clone can drive the prune task; the managed AppState
  takes the same actor.
- SCOPE / honesty (Simplicity First): true cross-restart *process* reaping of an
  orphaned agent process is NOT achievable in v1 — PIDs aren't persisted and the
  headless-supervisor daemon that would own surviving runs is explicitly post-v1.
  So v1 reaps the RECOVERABLE resources: the run's status (→ failed) and orphan
  worktree metadata (prune). A crashed run's worktree dir that still exists is
  left in place deliberately — the user may want to inspect it, its shadow refs
  preserve the diffable history, and prune only sweeps entries whose checkout
  vanished. Auto-removing live crashed worktrees is a heavier policy call left to
  the user / future work; it is not part of the bullet's parenthetical.
- Tests (1 new store, all pass): crash_recovery_fails_interrupted_runs_and_keeps_
  refs — two in-flight (running/queued) + one terminal (completed) run, one with
  a shadow-ref iteration; fail_interrupted_runs returns exactly [r1,r2], flips
  both to failed, leaves the completed run untouched, KEEPS r1's iteration row
  (refs intact), and is idempotent on a second call.

Verification:
- CARGO_HOME=/tmp/loopfleet-cargo cargo test --workspace — all pass (adapters 42
  +3 ignored, core 52, gitx 28, sandbox 13 +2 ignored, store 18 [+1 crash
  recovery]).
- cargo clippy --workspace --all-targets — clean, 0 warnings.
- cargo build --workspace — Finished (Tauri app links).
- The startup wiring itself runs in the Tauri setup() hook (can't be driven
  headlessly here); its store call is covered by the new test and cleanup_orphans
  by gitx's existing prune tests.

Next highest-priority task: M6 — Agent binary discovery + version checks, graceful
errors when a CLI is missing (the second M6 hardening bullet).

2026-07-08 — M6: Agent binary discovery + version checks + graceful missing-CLI errors
- Second M6 hardening bullet. Before this, launching a run with an uninstalled
  agent CLI cut a worktree, inserted a run record, spawned the loop, and only
  failed deep inside start_run when Command::spawn returned NotFound — leaving an
  orphan worktree and a run flipped running→failed with no clear cause. Now a
  missing CLI is a clean, up-front error; installed CLIs surface their version
  and warn on drift from the version the adapter was tested against.
- crates/adapters/src/discovery.rs (new): the per-agent CLI facts + discovery.
  - AgentSpec { key, display, binary, version_arg, tested_version } and
    KNOWN_AGENTS: the three v1.0 agents (claude/pi/cursor), keys matching
    build_adapter + the stored run `agent`, tested_version = the version each
    adapter was captured/tested against (claude 2.1.201, pi 0.80.3, cursor-agent
    2026.07.01, from the M1 adapter notes).
  - spec_for(key): lookup, accepts the "cursor-agent" alias for "cursor"
    (mirrors build_adapter's dispatch).
  - discover(spec) -> AgentStatus: one `<binary> --version` spawn. A NotFound
    spawn error → installed:false with "'<bin>' not found on PATH"; any other
    spawn error → installed:false with the error; success → installed:true,
    version = first version-like token (prefers stdout, falls back to stderr for
    CLIs that print the banner there). version_matches = Some(detected ==
    tested). Discovery is the single subprocess: presence + version in one shot,
    no separate PATH walk.
  - discover_all(): sequential over the three agents (three fast --version
    spawns — no join-all dep pulled in).
  - AgentStatus derives Serialize (added `serde` to adapters deps) so the Tauri
    command returns it directly. extract_version is a pure `\d+(\.\d+)+` scanner
    (no regex crate): first dotted number, at least one dot, not trailing-dot —
    handles "2.1.201 (Claude Code)", "pi version 0.80.3", the date-shaped
    "2026.07.01", strips a "-beta" suffix, rejects a bare "7" or "2.".
  - DESIGN: a version MISMATCH is a warning, never a blocker — the CLIs ship
    weekly (PRD Risks) so pinning exact-match would false-alarm constantly, but
    the drift is worth showing. Only a MISSING binary blocks a launch.
- crates/adapters/src/lib.rs: mod discovery + re-exports (discover, discover_all,
  spec_for, AgentSpec, AgentStatus, KNOWN_AGENTS).
- src-tauri/src/lib.rs:
  - launch_run: right after build_adapter and BEFORE any worktree/run-record
    work, spec_for(&agent) → discover(); if !installed, return the status.detail
    string. Fail-fast, so no orphan worktree / phantom failed run for a missing
    CLI. The existing frontend launch handler already surfaces the Err via
    setMsg(..., "err"), so the graceful message shows with no JS change.
  - agent_status() command (async) → Vec<AgentStatus> via discover_all();
    registered in the invoke_handler.
- dist/index.html: an "Agents" status strip above Projects. loadAgents() (called
  on startup alongside refresh()) invokes agent_status and renders one chip per
  agent: green dot + version when installed, a yellow "tested <v>" warning (with
  a tooltip) when the detected version differs, red dot + reason when missing.
  New CSS for #agents/.agent/.dot/.ver/.warn. Dependency-free vanilla JS.
- SCOPE (Simplicity First): "version checks" = detect + display + drift warning,
  not an enforced compatibility matrix. "Discovery" = presence via the version
  spawn, not a bespoke PATH resolver. Enough for the daily-use guardrail the
  bullet asks for.

Verification:
- CARGO_HOME=/tmp/loopfleet-cargo cargo test --workspace — all pass (adapters 46
  [+4 discovery: version extraction, non-version rejection, spec/alias lookup,
  missing-binary graceful; +1 ignored live claude discovery], core 52, gitx 28,
  sandbox 13 +2 ignored, store 18).
- cargo clippy --workspace --all-targets — clean, 0 warnings.
- Frontend JS parses (new Function over the <script> block). The Tauri GUI can't
  be driven headlessly here; agent_status/discover are exercised by the adapter
  tests and the launch guard reuses the existing error surface.

Design notes:
- Discovery lives in adapters (per-agent CLI knowledge — binary name, version
  flag, tested version — belongs beside the adapters), NOT on the frozen
  AgentAdapter trait (which is start_run/open_session only). It's a free
  function set, so the trait signature the PRD froze in M1 is untouched.
- The launch guard runs discover() (a --version spawn) on every launch. Cheap
  and always current — no cached availability that could go stale between an
  install/uninstall and the next run.

Next highest-priority task: M6 — Settings: default agent, default iteration
count, concurrency cap, sandbox profile overrides per project. (The remaining
M6 bullets — codesign/notarize, naming/domain — are the deferred release items,
after the tool has earned its keep in daily use per the PRD build order.)

2026-07-08 — M6: Settings (default agent, default iterations, concurrency cap,
per-project sandbox overrides)
- Store: migration 0002_settings.sql adds a KV `settings` table (key TEXT PK,
  value TEXT) for global defaults and `ALTER TABLE projects ADD COLUMN
  sandbox_extra_writes TEXT NOT NULL DEFAULT ''` for per-project write
  overrides (newline-separated absolute paths). Appended to MIGRATIONS in
  lib.rs (append-only, never edit a shipped entry).
- crates/store/src/settings.rs: Settings { default_agent, default_iterations,
  concurrency_cap } with serde + Default (claude / 5 / 3). load_settings reads
  each key, falling back to Default per-key (so the table only records explicit
  changes); save_settings upserts each key (ON CONFLICT DO UPDATE).
  project_sandbox_writes / set_project_sandbox_writes read/replace a project's
  override list (blank lines dropped on both read and write). concurrency_cap
  0 = unlimited.
- crates/store/src/runs.rs: count_active_runs(conn) -> u32 counts
  queued|running runs across all projects — the value the launch path compares
  against the cap. Exported alongside the settings fns.
- crates/sandbox/src/lib.rs: RenderParams gains `extra_writes: Vec<PathBuf>`
  (per-project overrides), chained into render() after agent_dirs/temp_dirs so
  each entry becomes a validated+escaped (subpath …) grant. RenderParams::new
  inits it empty; existing callers (all via ::new) unaffected. Doc note warns
  never to list the parent .git here (commits stay app-owned).
- src-tauri/src/lib.rs: four commands — get_settings, save_settings,
  project_sandbox_writes, set_project_sandbox_writes (the setter rejects any
  non-empty relative path up front, since the Seatbelt boundary needs absolute
  subpaths and a bad override must never silently break the boundary).
  launch_run now, under its existing single db lock: (1) enforces the cap —
  load_settings + count_active_runs, rejects with a "stop a run or raise the
  cap" message when active >= cap (cap 0 skips the check); (2) reads the
  project's sandbox overrides and sets params.extra_writes so they land in the
  rendered profile. Registered the four commands in the invoke_handler.
- dist/index.html: a collapsible Settings panel (default agent select, default
  iterations + concurrency-cap number inputs, Save) wired to get_settings/
  save_settings via loadSettings() on boot. The per-task launch affordance now
  pre-selects settings.default_agent and settings.default_iterations (the iter
  dropdown injects the configured default into its options so a non-standard
  value is still selectable). Per-project sandbox overrides render as a
  collapsible <details> textarea at the top of the plan view (needs the
  selected project's context), saving through set_project_sandbox_writes with
  its own inline ok/err message. Added scoped CSS for both.

Verification:
- CARGO_HOME=/tmp/loopfleet-cargo cargo test --workspace — all green (store 21
  incl. 3 new settings tests, sandbox 14 incl. the extra_writes render test,
  gitx 28, adapters 46, core 52; ignored live/macos tests unchanged).
- cargo clippy --workspace --all-targets — clean, 0 warnings.
- cargo build -p loopfleet — Finished (Tauri app compiles with the new
  commands).

Design notes:
- Global settings as a KV table (not typed columns) keeps "add a setting" to a
  code change in settings.rs, no migration, and missing-key-means-default falls
  out for free.
- Per-project overrides live on the projects row rather than a side table: they
  are 1:1 with a project and read on the launch hot path, so a column read is
  the simplest thing that works. The Project struct + list/get queries were
  left untouched (they select explicit columns), so this stayed surgical.
- Concurrency is enforced at launch only (a soft admission gate), not tracked
  as a running semaphore — v1 has no queue; a rejected launch is an immediate,
  explained error, matching the "no pause, stop at boundary" run model.

Next: M6's remaining items are release/distribution chores explicitly deferred
until the loop tool is in daily use — codesign+notarize / DMG, and the naming
workflow (register domain, replace working name). M5 (plan chat) is likewise
deferred per the build order. No non-deferred tasks remain.

2026-07-12 — M7: Scaffold Vite + React + TS frontend, wire tauri.conf.json
- First M7 task (real React frontend on the unchanged Rust backend). A partial
  scaffold already existed under frontend/ (index.html, package.json,
  tsconfig*.json, vite.config.ts — untracked) but no src/ and tauri.conf.json
  still pointed at the legacy static ../dist with withGlobalTauri. Completed the
  scaffold and rewired Tauri.
- src-tauri/tauri.conf.json: build block now has frontendDist "../frontend/dist"
  (the Vite build output), devUrl "http://localhost:5173", and object-form
  beforeDevCommand/beforeBuildCommand ({ cwd: "../frontend", script: "npm run
  dev"|"npm run build" } — cwd is relative to the config file so the tauri CLI
  runs npm in frontend/). Removed app.withGlobalTauri — the React app calls
  @tauri-apps/api directly (invoke from @tauri-apps/api/core), no global bridge.
- frontend/src/main.tsx: React 19 root (createRoot + StrictMode) mounting App
  into #root (the existing index.html already had <div id="root"> and the
  /src/main.tsx module script).
- frontend/src/App.tsx: minimal scaffold surface — on mount, invoke<Project[]>
  ("list_projects") and render repo_path + plan_convention, with an error line
  and an empty state. Deliberately minimal: the design system, hand-maintained
  types.ts/commands.ts, and the real views are the following M7 tasks; this one
  only proves the app boots and reaches the unchanged command surface via
  @tauri-apps/api. Project type inlined here (typed wrappers come next task).
- frontend/tsconfig.node.json: dropped "noEmit": true — a referenced *composite*
  project may not disable emit (tsc TS6310); it broke `tsc && vite build`.
  Composite requires emitting build info; kept "strict". (The pre-existing
  scaffold had added strict+noEmit here.)
- .gitignore: added /frontend/node_modules and /frontend/dist (build output +
  deps not committed).
- The legacy static dist/index.html is left in place — it is deleted by the last
  M7 task once parity is reached, not now. frontendDist now points at
  frontend/dist, so the built React app is what a bundle would ship.

Verification:
- cd frontend && npm install — 69 packages, 0 vulnerabilities.
- npm run build (tsc && vite build) — tsc clean, vite built frontend/dist
  (index.html + assets/index-*.js, 30 modules).
- CARGO_HOME=/tmp/loopfleet-cargo cargo build -p loopfleet — Finished. This
  matters: build.rs runs tauri_build::build(), which parses/validates
  tauri.conf.json at compile time, so a clean build confirms the new devUrl /
  before-commands / frontendDist config is valid.
- `tauri dev` boots is NOT verifiable here (no tauri CLI installed, no display in
  this environment). The checkable portion — conf validates + frontend builds —
  passes; the interactive boot is left for a desktop run.

Next highest-priority task: M7 — types.ts mirroring the command payloads +
typed commands.ts/events.ts wrappers over invoke and the run_event/run_status
event streams.

2026-07-12 — M7: types.ts + typed commands.ts/events.ts wrappers
- Second M7 task: hand-maintained TypeScript mirror of the command surface (PRD
  decision: no codegen in v1 — small, stable surface) plus one typed wrapper
  layer over invoke and the two live event streams. No Rust touched; backend
  command signatures are byte-for-byte unchanged.
- frontend/src/types.ts: every serde-Serialize payload the command layer returns
  or emits, field names matching the Rust serde output (snake_case), each type
  annotated with its source struct so drift is traceable:
  - store: Project, Settings, RunSummary. adapters: AgentStatus (version/
    version_matches/detail are nullable — Option<...> in Rust).
  - core: TaskStatus (kebab-case union: not-started | in-progress |
    completed-unaccepted | accepted), TaskView, PlanView, RunStatus (the
    RunState::as_str tokens), FileChangeView, DiffView, TimelineEvent,
    IterationView, RunTimeline, RunCompare, CompareView, Usage.
  - NormalizedEvent as a discriminated union on `kind` (snake_case tag), one
    arm per Rust variant — the internally-tagged {"kind":…} shape the store
    persists and the run_event stream carries; command_run.exit and the nullable
    Option fields modeled as `T | null`.
  - src-tauri payloads: UseRunResult, RunEventPayload, RunStatusPayload.
  - NB (per PRD "note it, don't silently widen"): the plan-view task type is
    actually TaskView (anchor/line_hint/text/checked/status/run_count), not the
    PRD's shorthand "Task" — mirrored as-is, the command surface is unchanged.
- frontend/src/commands.ts: exactly one typed async wrapper per command in
  src-tauri's generate_handler! (register_project, list_projects, agent_status,
  get/save_settings, project/set_project_sandbox_writes, plan_overview,
  launch_run, stop_run, plan_runs, run_timeline, compare_task, use_run). Arg
  keys are camelCase (Tauri v2 maps them to the Rust snake_case params — matches
  the legacy dist/index.html calls: runId, projectId, taskAnchor, …). This is
  the only module that calls invoke.
- frontend/src/events.ts: onRunEvent / onRunStatus over listen<T>("run_event")
  / listen<T>("run_status"), each returning the UnlistenFn promise for effect
  cleanup.
- frontend/src/App.tsx: rewired off the inlined Project type to import from the
  wrappers — listProjects() (one command) + onRunStatus subscription with
  cleanup (one live event stream), satisfying the task's round-trip criterion at
  the wrapper boundary.

Verification:
- cd frontend && npm run build (tsc && vite build) — tsc CLEAN (strict,
  noUnusedLocals/Parameters), vite built frontend/dist (33 modules). The tsc
  pass is the real check: every wrapper's arg/return type and the App.tsx
  consumption type-check against types.ts.
- Live tauri dev round-trip (actually invoking the command + receiving an event
  in the WebView) is NOT verifiable here (no tauri CLI / display in this env,
  same limitation as the scaffold task). The compile-time half — tsc clean,
  wrappers consumed by App — passes; the interactive round-trip is left for a
  desktop run.
- Backend untouched: no Rust files changed, tauri.conf.json unchanged, so no
  cargo build needed.

Next highest-priority task: M7 — App shell + design system (sidebar/main layout,
design tokens, the honest sandbox-boundary trust panel).

2026-07-12 — M7: App shell + design system + sandbox-boundary trust panel
- frontend/src/tokens.css: design tokens as CSS custom properties (PRD M7
  "color, type scale, spacing, radii") + a small base/reset. Single dark theme
  (cockpit tool, reads best dark). Palette: layered surfaces bg→surface-3 +
  borders, text/muted/faint, accent + ok/warn/danger semantics. Type scale 1.25
  ratio off 13px base (xs..xl), sans + mono stacks. 4px spacing scale, three
  radii, --sidebar-w layout var. No view hardcodes a raw color/size/radius.
- frontend/src/shell.css: structural classes only (values via tokens) for the
  shell grid, sidebar (brand, section label, scrollable project list, project
  item with active/hover states, empty state), main pane (header + body +
  placeholder), an error banner, and the sandbox panel.
- frontend/src/components/AppShell.tsx: presentational sidebar/main grid; owns no
  data, takes `sidebar` + `children` slots so later M7 views render into it.
- frontend/src/components/SandboxBoundaryPanel.tsx: the trust feature. States the
  boundary plainly per the PRD Sandbox section — Confined: writes limited to the
  worktree + progress dir, parent .git never writable (app-owned commits);
  Open: reads & network NOT confined, agent can read ~/.ssh/~/.aws/other repos'
  env and POST out. Foot note: read-scope + egress hardening are post-v1.
- frontend/src/App.tsx: composition root — loads projects into the sidebar
  (selection state, auto-selects first), main header shows short repo name +
  full path, main body carries a placeholder pointing at the coming views plus
  the SandboxBoundaryPanel. Dropped the scaffold's onRunStatus demo listener
  (round-trip already proven in the prior task; not part of the shell).
- frontend/src/main.tsx: import tokens.css + shell.css.

Scope: pure frontend, shell only. Deliberately did NOT build the projects
component (add-project dialog/chips), plan view, or run surfaces — those are the
next M7 tasks. Backend untouched: no Rust, no tauri.conf.json changes.

Verification:
- cd frontend && npx tsc --noEmit — CLEAN (strict, noUnusedLocals/Parameters).
- npm run build (tsc && vite build) — built; CSS bundled into
  frontend/dist/assets (index-*.css ~5KB), 35 modules. The
  "writes-confined / reads+network-open" statement renders in the panel.
- Live tauri dev (visual match to a reference, in-window render) NOT verifiable
  here — no tauri CLI/display in this env, same limitation as prior frontend
  tasks. Compile + bundle half passes.

Next highest-priority task: M7 — Projects + Agents-status + Settings as
components (add-project dialog, agent availability/version-drift chips, settings
form, per-project sandbox overrides).

2026-07-12 — M7: Projects + Agents-status + Settings as components
- Fourth M7 task: turned the overview surface (add-project, agent chips,
  settings, per-project sandbox overrides) into typed React components on the
  unchanged Rust backend. No Rust touched; no tauri.conf.json/capability change.
- frontend/src/components/AddProject.tsx: the add-project affordance. Native
  folder picker via @tauri-apps/plugin-dialog `open({ directory: true })` (the
  same dialog plugin the legacy static UI used — already registered on the Rust
  side and covered by the existing `dialog:default` capability, which includes
  allow-open in plugin-dialog v2, so no capability edit was needed) → the
  unchanged `register_project` command. Registration errors (NotAGitRepo /
  AlreadyRegistered, returned as strings by core) surface inline; a cancelled
  picker is a no-op. onAdded callback hands the new Project up to App.
- frontend/src/components/AgentStatusPanel.tsx: agent availability + version
  drift, one chip per agent from `agent_status`. Green dot + version when
  installed; yellow dot + "tested <v>" warning (title tooltip) when
  version_matches===false; red dot + detail when missing. Mirrors the legacy
  chip semantics as a component.
- frontend/src/components/SettingsPanel.tsx: default agent (select over the v1
  agent keys), default iterations, concurrency cap (0=unlimited). Loads via
  `get_settings` on mount (falls back to in-state defaults on failure), saves
  via `save_settings` with an inline ok/err message; clamps iterations>=1 and
  cap>=0 before saving (matches the legacy Math.max guards).
- frontend/src/components/SandboxOverrides.tsx: per-project extra write paths,
  one absolute path per line, scoped to the selected project (reloads on
  projectId change). Reads `project_sandbox_writes`, writes
  `set_project_sandbox_writes` (Rust setter rejects relative paths → inline
  error, so a bad override can't silently weaken the boundary). Hint states
  never to list the parent .git (commits stay app-owned).
- frontend/src/panels.css: the shared primitives these components use — .panel,
  .btn/.btn--accent, .msg/.msg--ok/--err, .form-grid/.field, .agent-chip* (dot
  colored by on/drift/off), .overrides__ta, .add-project*, .overview stack.
  Structural only; every value via tokens.css (no hardcoded color/size/radius),
  consistent with shell.css.
- frontend/src/App.tsx: composition — AddProject sits under the sidebar
  "Projects" label; onAdded appends the returned Project and selects it. The
  main body is now an .overview column: AgentStatusPanel, SettingsPanel,
  SandboxOverrides (only when a project is selected — it needs project context),
  then the existing SandboxBoundaryPanel. Replaced the shell placeholder text.
- frontend/src/main.tsx: import panels.css alongside tokens.css/shell.css.
- frontend/package.json: added @tauri-apps/plugin-dialog ^2 (frontend-only dep;
  the Rust plugin was already present from M0).

Scope (Simplicity First): built exactly the four overview pieces this bullet
names, as components. Did NOT touch the plan view, run surfaces, or backend —
those are later M7 tasks. Agent select uses a small constant key list rather
than deriving from agent_status (keeps SettingsPanel from double-loading).

Verification:
- cd frontend && npx tsc --noEmit — CLEAN (strict, noUnusedLocals/Parameters).
- npm run build (tsc && vite build) — built frontend/dist (41 modules;
  index-*.css 7.82KB, index-*.js 202.81KB). tsc is the real check: every
  component's props, command args/returns and App composition type-check
  against types.ts/commands.ts.
- Live tauri dev (the native folder picker opening, chips/settings/overrides
  round-tripping in-window) is NOT verifiable here — no tauri CLI/display in
  this env, same limitation as the prior frontend tasks. Compile+bundle passes;
  the interactive round-trip is left for a desktop run.
- Backend untouched: no Rust files, tauri.conf.json, or capability changes, so
  no cargo build needed.

Next highest-priority task: M7 — Plan view: task list with derived TaskStatus
badges; launch control (agent + iterations + Run) on EVERY task regardless of
`checked`; completed-unaccepted surfaced loudly (the review queue).

2026-07-12 — M7: Plan view (task list, status badges, per-task launch control)
- frontend/src/components/PlanView.tsx: the plan surface for the selected
  project. Loads plan_overview(projectId) → PlanView[] and renders each plan as
  a card. Per task: the derived TaskStatus badge (not-started / in-progress /
  completed-unaccepted="Needs review" / accepted), the task text, an authored-
  checked marker (muted, non-blocking), a run_count chip, and a launch control.
- Launch control is on EVERY task regardless of `checked` (the PRD's core M7
  fix). `checked` only ever gated derived status, never runnability — the badge
  reads it, the Run button ignores it — so a "done" plan (all boxes checked)
  still shows a Run button per task. Control = agent <select> (installed agents
  only, from agent_status) + iterations <input> + Run. Defaults resolve from
  get_settings (default_agent if installed, else first installed; default_
  iterations), adopted via empty-string sentinels so a user's choice is never
  clobbered when settings arrive late. launchRun(projectId, taskAnchor, agent,
  maxIterations); on success shows "Launched" and reloads plan_overview so the
  new run's status/run_count appears. No agent installed → control disabled with
  a "No agent CLI is installed" title (matches the backend's fail-fast refusal).
- Completed-unaccepted surfaced loudly: a per-plan review banner ("N runs are
  awaiting review…") when any task is completed-unaccepted, plus a warn-colored
  border on each such task row. This is the compare/accept backlog the PRD wants
  foregrounded.
- frontend/src/plan.css: plan-card, review-banner (warn), task-list/task-row,
  task-badge--<status> (one color per status), and the launch control styling.
  Structural only; every value via tokens.css. Imported in main.tsx.
- App.tsx: main body now renders <PlanView key={project.id}> for the selected
  project above the existing overview panels (agents/settings/sandbox), or a
  placeholder when no project is selected. `key` remounts cleanly on project
  switch. No backend command signatures touched — only plan_overview, get_
  settings, agent_status, launch_run, all pre-existing.
- Scope: only this M7 task. The run the button launches is not yet viewable —
  the global run surface, live run view, timeline, and compare are the next M7
  tasks. This task delivers "you can always start a run, from any task."

Verification:
- cd frontend && npx tsc --noEmit — clean.
- npx vite build — built (43 modules), no errors.

Next highest-priority task: M7 — Global run surface: a persistent dock/panel
listing active runs across projects, each opening its live view (the always-
present "you can run agents here" entry point).

2026-07-12 — M7: Global run surface (persistent run dock)
- frontend/src/components/RunDock.tsx: a persistent bottom dock listing every run
  launched this session, across projects. Each run shows a status chip
  (queued/running/completed/failed/stopped), the task text, and agent · project.
  Active runs (queued/running) get a Stop button; terminal runs get a Dismiss
  (✕). Empty state prompts to launch. Exports the ActiveRun type. Clicking a run
  fires onOpen (selection) — the live view it opens lands in the next M7 task, so
  onOpen here only carries the selection + highlights the chip.
- Scope decision (recorded in the file's header comment): v1 runs do NOT survive
  an app restart (M6 crash recovery marks still-running runs failed on startup)
  and there is no global "active runs" backend command, so the dock's registry is
  exactly the runs launched in this session. This is complete and honest for v1;
  no backend command was added (PRD M7: "if a view needs data the backend doesn't
  expose, note it — don't silently widen the command surface"). Only pre-existing
  commands used: launch_run (already), stop_run, and the run_status event stream.
- frontend/src/components/AppShell.tsx: added a third `dock` slot rendered below
  the sidebar/main grid. Shell grid gained a second row (minmax(0,1fr) auto) so
  the dock spans full width at the bottom, OUTSIDE the scrolling main pane — the
  verify criterion ("minimize the plan, the run is still visible and stoppable")
  is met structurally: main scrolls, the dock does not.
- frontend/src/dock.css: dock + run-chip styles, all off tokens.css (status
  colors reuse accent/ok/danger/warn). Imported in main.tsx.
- frontend/src/App.tsx: owns the session run registry (runs: ActiveRun[]) and
  selectedRunId. onLaunch(run) prepends a new ActiveRun (status "running",
  tagged with the selected project's short name). Subscribes onRunStatus once to
  flip a run's status to its terminal state. Renders <RunDock> in the shell's
  dock slot: onOpen=setSelectedRunId, onStop=stopRun (errors surface in the
  banner), onDismiss removes the run from the registry.
- frontend/src/components/PlanView.tsx: threaded a new onLaunch(LaunchedRun)
  callback down PlanView → PlanCard → TaskRow → LaunchControl. LaunchControl now
  captures launch_run's returned run id and reports { runId, taskText, agent }
  upward; the existing onLaunched() (plan reload) is unchanged and still fires.
  Exported the LaunchedRun type.

Verification:
- cd frontend && npx tsc --noEmit — clean.
- npx vite build — built (47 modules), no errors.
- Backend crates + Tauri command signatures byte-for-byte unchanged (no Rust
  files touched).

Next highest-priority task: M7 — Live run view component: run_event/run_status
subscription, streaming events, live file-changes, Stop (the view the dock's
onOpen will render).
