//! loopfleet core: supervisor, run loop, and the normalized event enum.
//!
//! This crate owns the domain types shared across the app. Concrete
//! functionality lands with M1 (events + adapters) and M3 (the loop).
